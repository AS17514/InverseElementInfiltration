# 前端待办

> 当前唯一前端任务源。跨端 / 策划决策见 `docs/待确认.md`。
> 已完成事项归档：`docs/archive/前端待办-已完成事项.md`（本文件只保留未完成任务、前置条件与人工验收项）。

> 😩 **吐槽（2026-08-23，后端代发）**：前端待办越来越多了……感觉会好辛苦……谁把我的美好时光当海苔给吃掉了？
> 我才看到，啥时候积这么多了🥲🥲🥲

## 高优先级（未完成）

- [ ] **牌库面板**（`Btn_Graveyard` 已改显示名"牌库"并解析引用；`DeckLibraryPanel.cs` Key="DeckLibrary" 已实现——打开读快照+监听 PieceDied/PiecePromoted/HandChanged/StateChanged("mahjong-wall") 刷新、`Btn_Confirm`/点背景关闭，UIPanels 已注册）：**⚠️ 2026-08-26 审计：Bootstrap 入口接线缺失**（`OnDeckClicked` 无订阅、`PushOverlay("DeckLibrary")` 未接线——按钮点击当前无效）；**另：容器无 Mask/未接滚动（卡多会溢出）、麻将牌不显示**（`!card.IsPiece` 跳过，同手牌口径）。后端数据源见下。
  - **⚠️ 抽牌堆显示·C1 拍板（2026-08-25 用户）**：抽牌区（`GameState.DrawPile`——**随机抽取、无牌序**）显示 = **全部牌正面**（棋子牌立绘+名 / **麻将牌也显示正面点数**——突破"麻将牌不显示"口径，仅限抽牌区）+ **每次打开面板/切页时随机打乱排列**（不暗示抽取顺序）+ 顶部数量角标。实施责任：DeckLibraryPanel；详见 `docs/待确认.md`。
  - 后端数据源已就绪（只读；**面板打开读一次快照即可**，实时同步监听现有事件即可，无需新事件）：**弃牌堆 = 墓地** = `GameState.Graveyard`（玩家棋子死亡 defId=升变后形态）；另有 `PromotedReplacedPile`（升变替换池）、`MahjongUsedPile`/`MahjongDeadPile`（麻将使用/死亡池）、`DiscardZone.PieceDeaths/MahjongDeaths`（弃牌区卡片化记录）——变化事件 `PieceDied`/`PiecePromoted`/`StateChanged("mahjong-wall")`。（2026-08-25 复核：麻将死亡双写已补齐——墙体破坏/摸切均写 `DiscardZone.MahjongDeaths`，与 `MahjongDeadPile` 并存，展示任选其一。）
- [ ] **新手教程（框架已搭 2026-08-26，待人工验收 + 李毕拼面板）**：覆盖剧情后事件界面/遗物 → 棋子编辑 → 牌组构筑 → 战斗（手牌/部署/信息区/行动点/友伤）。
  - ✅ **代码级已实现**：`TutorialManager.cs`（观察既有事件只读触发：EventOpened/AbilityCandidatesDrawn/RuleCandidatesDrawn→事件教程、RelicObtained 等待步、EditCandidatesDrawn→编辑、StateChanged("deck")→构筑、PhaseChanged(Placement)→战斗；会话内去重；等待事件 8s 超时跳过；序列排队）+ `TutorialPanel.cs`（Key="Tutorial"，角色说话：头像+文本框两独立框体、打字机、任意键下一步/方向键上左/PageUp 上一步/长按 1.5s 跳过、布局预设 8 种、DOTween 移动+屏幕内 clamp、无 prefab 时纯代码兜底）+ `TutorialMask.cs`+`TutorialMask.shader`（shader 挖孔遮罩：任意 UI/场景对象、多目标联合挖孔、padding 可调、DOTween 缓动）+ `TutorialContent.cs`（四序列文案，源=策划 docx，占位符待策划补全）+ Bootstrap 接线（Awake 创建、StartNewGame 标记新局）。遮罩 shader 已加入 Always Included（GraphicsSettings）。
  - **李毕契约（拼 prefab 节点名）**：面板 Key="Tutorial"、prefab 放 `Assets/_Project/UI/Prefabs/TutorialPanel.prefab` 并注册 UIPanels 组；节点：`Grp_Portrait`+`Img_Portrait`、`Grp_Text`+`Txt_Speaker`+`Txt_Text`（两框独立、布局预设已定义间距）。
  - **高亮目标名待确认**：`@handArea/@frontRows/@firstPlayerPiece/@leftInfo/@bottomLeft/@bottomRight` 与 EventPanel/PieceEditPanel/DeckBuildPanel 根名——代码按候选名表解析，未命中跳过不高亮（不阻塞），李毕确认节点名后填入 `TutorialManager.ResolveDynamic`。
  - **⏳ 崩溃阻断项（2026-08-25 李毕实测）：`TutorialPanel` 未注册 Addressables**——Awake 创建 TutorialManager → `LoadAssetAsync("TutorialPanel")` → InvalidKeyException 炸 Awake。修复 = **TutorialPanel.prefab 拼好并注册进 Addressables（UIPanels 组）**（李毕契约见上）；**临时绕过**：`TheLaw.Core.Tutorials.Enabled = false`（后端已提供——关闭后 Bootstrap 不创建 TutorialManager，教程完全不运行、记录不污染；发布前置回 true）。
  - **后端持久化契约**：见 `docs/后端待办.md`（TutorialSystem.TryShow 发 TutorialRequested 事件 + 流程点调用）——未接前前端会话内去重（重启重放，可接受）。
  - 验收：新局剧情后事件界面弹角色说话+高亮事件面板；选遗物后出现遗物介绍；编辑/构筑/战斗各段按 docx 顺序触发；任意键翻页、上一步、长按跳过、框体屏幕内不越界；遮罩中间透出目标四周压暗且间距可调。
- [ ] **新玩法前端接入**（2026-08-24 后端代码就绪——骰子/围棋/代币，仅玩家侧；**2026-08-24 审计：前端三件 UI 均未接**；**激活入口 = 玩法事件二选一（2026-08-24 定案——见下方「玩法事件·二选一面板」条目；此前"每关固定玩法"记录已作废）**）：
- ✅ **骰子前端接入（2026-08-24 已实现，待人工验收）**（`GameState.DiceValue`）：投掷按钮 `Btn_RollDice`（执行类行动 1 AP，发 `RollDiceRequest`）；“点数直线移动”启动按钮 `Btn_DiceMove`（发 `DiceMoveRequest`，不耗 AP 消耗点数）→ `StateChanged("dice-move-select")` → **场上高亮可达格**（4 向点数步直线终点，绿块与移动选格同视觉——用户定案替代方向键）→ 点格反推方向 → `BattleFlow.OnDiceDirectionSelected(Direction)`；点数显示 `Txt_DiceValue`、提示 `Txt_DiceHint`、面板显隐并入 `Grp_Play` 容器（选中棋子隐藏、取消恢复——李毕契约）；buff 提示不跨回合。验收：骰子玩法激活后投掷→点数显示→启动移动→高亮可达格→点格直线移动。
  - **围棋**：部署入口（常驻"棋子牌"——后端 `GoPiece` 代码内建，独立于手牌区；发 `DeployGoRequest(cell)`——不耗 AP、任意空格；退场复用 `PieceDied` 表现）。**⚠️ 次数显示契约（2026-08-24 买子落地后必做）**：入口需展示**本回合剩余可用部署次数** = 免费限次（`GameState.GoDeployLimit()`：默认 1、速攻→2）＋ 买子次数（`GameState.GoExtraDeploys`）− 已用（`GameState.GoDeployCount`）；**买子按钮**（能力「买子」持有且围棋+代币玩法激活时显示）："花 2 代币 +1 次"（发 `BuyGoRequest`；费用与余额校验后端做，前端仅显示 `TokenCount` 不足置灰）；刷新时机：`StateChanged("go")`（部署/购买/回合开始后）+ 回合开始。能力「速攻」/「升值」/「假定」/「买子」存在时入口提示对应能力（速攻=2 次/升值=场上围棋价值计数 `GoValueBonus`/假定=可升变/买子=可购买）。
  - ✅ **围棋入口已实现（2026-08-26，待人工验收）**：`Grp_FloorPlay_Go`（李毕已拼）手牌式拖拽部署——拖 `Piece_Handcard` 到任意空格（非占用/非障碍/界内）→ `DeployGoRequest(cell)`；剩余次数（`GoDeployCapacity()-GoDeployCount`）、下次颜色（蓝红切换）、提示文本实时刷新；面板与棋子信息区同位置切换显隐（选中隐藏/取消恢复）；刷新挂 `StateChanged("style"/"go")` + 回合切换（RefreshAll）+ RelicObtained。**买子按钮已接线（2026-08-26，待人工验收）**：`Btn_BuyGo` 已接——能力「买子」持有 && 围棋+代币玩法激活时显示，文本"花 X 代币 +1 次"（X=遗物 effect `TokenBuyGo.value`，前端只读镜像），`TokenCount` 不足置灰；点击发 `BuyGoRequest`（后端扣币 + `GoExtraDeploys`+1 + `StateChanged("go")`）；**hover 浮窗**：未激活代币玩法 → "买子需要激活代币玩法"、代币不足 → 提示差额、正常 → 说明作用；刷新挂 `StateChanged("go"/"token")`。**能力提示已实现**（2026-08-26：`Txt_Hint`=动态能力提示，按持有能力显示（速攻=每回合 N 次/升值=场上围棋价值 +N/假定=围棋可升变/买子=花 N 代币 +1 次），空=无能力；`Txt_Tip` 常驻文本预制体预设——代码不写）。验收：持买子能力且双玩法激活时按钮出现/置灰正确、hover 提示原因、点击后次数+1 代币-1。
  - - - **代币**（`GameState.TokenCount`）：购买面板（发 `BuyTokenRequest(discardIndex)`——读弃牌区 `DiscardZone.PieceDeaths/MahjongDeaths` 选牌；显示牌价值与所需代币；复制入手牌）。✅ **2026-08-24 已接（待人工验收）**：点购买 → 复用**牌库面板购买模式**（`DeckLibraryPanel.EnterBuyMode`——标题改"选择要购买的牌"、仅弃牌区牌可点击、点击=购买该牌并关闭面板、普通浏览恢复）；牌库面板已在 Bootstrap 常驻注册；代币数量随 `StateChanged("token")` 刷新。⚠️ 已知限制：弃牌区仅显示棋子死亡卡（麻将死亡卡不在面板——麻将卡表现未做，暂不可买）；Btn_Graveyard（牌库浏览按钮）事件仍未接（另条待办）。验收：代币玩法激活 → 购买弹牌库、标题为提示、点弃牌区牌扣币入手牌并关面板、代币不足后端拒绝。
  - 请求无需新传参（只带 cell/discardIndex）；新状态全部公开可读。
- [ ] **麻将牌表现拓展（2026-08-24 记入——麻将玩法 UI 缺口；2026-08-25 牌库面板/代币购买暴露）**：麻将牌（`Card.IsMahjong`，点数卡）目前**手牌区不显示**（`BattleController` 手牌只渲染棋子牌）、**牌库面板弃牌区不显示**（`BuildPile` 跳过非棋子卡）→ ① 手牌区麻将牌卡（显示点数；点选/拖拽打墙：发 `PlayMahjongRequest(cell)` 棋盘选两格，1 AP——`BattleFlow.OnPlayerRequestPlayMahjong` 已就绪）；② 牌库面板支持麻将死亡卡（弃牌区 = `DiscardZone.PieceDeaths + MahjongDeaths`，卡片模板需支持点数展示）→ 代币购买**可买麻将牌**（索引对齐 `Resolver.DiscardView`）；③ 麻将卡模板样式待李毕（复用现有卡 or 新拼）。依赖：麻将卡 prefab/模板；后端 `PlayMahjongWall` 已就绪。

- [ ] **能力交互面板 ×2（2026-08-24 后端已就绪，能力池 P1）**：① **宝牌选数**——获得「宝牌」能力后（RelicObtained，或三选一落账后）弹 **1-9 数字选择面板** → `Resolver.SetBaopaiNumber(n)`（后端校验持有+1-9）；② **出千选数**——投掷时收到 `StateChanged("dice-rig-select")` → 弹 **1-6 自选面板** → `BattleFlow.OnDiceNumberSelected(n)`；**不跨回合**（未选则作废，下一回合消失）。面板形态可复用能力事件候选样式（简单按钮网格）。（2026-08-26 审计：两面板均未实现——仅后端信号 `SetBaopaiNumber`/`dice-rig-select` 就绪）
  - **策划原文**（口述文档：`内部文档/方案与配置器/能力池完整清单_策划口述_20260824.md`）：宝牌 = "游戏开始时，选择一个1—9的数字，本局游戏中那个数字对应价值的牌将被视为'宝牌'，'宝牌'进入弃牌区时基础得分＋其价值数，参与凑成刻子或顺子时番数额外＋1"；出千 = "行动点+1 投掷骰子的点数可以自选"。
  - ⚠️ 两个面板就绪前，对应能力照常可被选中（宝牌/出千入口缺面板 = 能力"有勾选但无生效交互"——宝牌会一直等选数、出千会一直等自选；**面板未接期间建议先不把这两条加入候选池**或前端按缺省值兜底（宝牌默认随机数字 / 出千默认随机投掷），具体取舍前端定——后端无阻塞）。

- [ ] **震击墙表现（2026-08-24 后端已就绪，能力池 P3）**：`StateChanged("shock-walls")` 事件（开战生成时发）→ 在棋盘渲染 2 个不可破坏墙（区别于麻将墙：不可破坏 + 攻击溅射）；攻击溅射伤害走既有 `DamageDealt` 逐目标飘字（Damage=1、FriendlyFire=true——与 AOE 同款，复用现有表现即覆盖）。后端状态读取：`GameState.ShockWalls`（格子集合，战斗级）。（2026-08-26 审计：溅射已走 Resolver→`DamageDealt` 逐目标飘字；**墙渲染缺失、`shock-walls` 事件 UI 无监听**——待补）

**回合结束手动化（2026-08-23 后端已实施，前端零改动）**：AP 耗尽不再自动结束回合——玩家手动点"结束回合"（无需改）；可选增强（ap-empty 提示）已实施，见待验证栏。

## 低优先级 / 暂缓

- [ ] **弃牌区 / 抽牌堆点击查看**（2026-08-24 提议——**待与策划讨论**，先放着不动；后端数据已就绪）。
- [ ] **ESC 关闭面板（2026-08-25 建议）**：当前无面板支持 ESC 关闭；牌库面板等弹窗后续统一（先按面板外点击关闭）。
- [ ] **谢幕面板**：首次通关后的独立 Thanks for playing / 结局报幕；不属于普通战斗结果面板。
- [ ] **表现队列收尾防御**：未来表现链复杂后，销毁控制器前等待表现队列排空；当前 `ResetState` 已兜底，无已知可达故障。

## 待验证（已完成·待人工验收）

> 已完成（代码级证据/已实施）但尚未人工验收（Play 实测）的事项集中于此；验收通过后移回「本轮已完成」并归档。
- ✅ **主菜单开场动画 v2（2026-08-26 已接，待人工验收）**：冷启动首次进主菜单播放（返回主菜单不播）——BGM 3s 渐入；主标题 → 副标题 → 背景缓亮（纯黑→#808080）+ 按钮组（CanvasGroup 控透明，按钮恒可用态）三段依次渐入、相邻段 0.5s 重叠，总约 7.5s（无闪动）；动画期全屏透明阻射线层，任意键/鼠标左键/触摸点击 = 跳过直落终态（BGM 满音量）。**启动即黑屏**：Bootstrap Awake 首帧创建全屏黑罩（ScreenSpaceOverlay）盖住所有面板异步加载过程，主菜单开场动画接管黑屏后销毁。实现：MainMenuPanel 开场协程 + AudioManager.PlayBGM(fadeSeconds)/CompleteBGMCrossfade + Bootstrap 黑罩。验收：冷启动全程无加载闪现、动画节奏/重叠、跳过、退出战斗回主菜单不播动画。
- ⚠️ **Bootstrap.cs 截断修复（2026-08-26，用户批准）**：工作区 Bootstrap.cs 曾因他人 WIP 保存截断（927 行中断，UI 程序集编译失败）；经批准用 HEAD 尾部补全（保留 WIP 前 927 行全部插入）+ 补 CreateLoadingPanel（协作者新增调用、方法体随截断丢失——按同款常驻创建模式补全）。编译 0 错误 0 警告。⚠️ 协作者：若本地另有完整版 WIP，请以当前文件为准或 diff 核对，勿整文件覆盖。

- ✅ **战斗场景前后层级统一排序**：棋子按行排序、阴影最底、手牌50/列表60/Tooltip1000 分层排序已代码配置。验收：常驻信息不被遮挡、弹窗可置顶。
- ✅ **行动经济 buff 显示**：BuffDisplay 按棋子输出 action_economy/action_economy_acted（ActionEconomyBuffSync 订阅既有事件补发刷新）。验收：获能力后显示态 A，执行后态 B，新回合回态 A。
- ✅ **插入执行（免费行动）UI 形态**：提示条+自动锁定棋子+弹目标选择+允许空放（TryStartForcedExec/ShowFreeActionTip）。验收：击杀触发→提示、锁定、目标选择、空放、自动收尾。
- ✅ **回合结束手动化可选增强（ap-empty 提示）**：BattleController 消费 StateChanged("ap-empty")，结束回合按钮 Tooltip 提示（1.6s 自动消失）。
- ✅ **通用变亮通道接入**：监听 HintRequested；CardQualify 手牌变亮+能力面板金色脉冲，按 EditedCardQualifyId 回填。
- ✅ **能力事件显示修订**：描述区去掉候选清单；遗物图标改代码色块；能力栏兼容回退 displayName。
- ✅ **战斗音效补挂**：玩家判负 SfxDeathUi（BattleResultPanel）+ promote/draw/shield/free/score 复核在位（音频挂点清单 2-6 条）。
- ✅ **UI/事件面板碰撞音**：UiSfx.Play 统一碰撞音覆盖 12 文件 23 处（音频挂点清单第 7 条）。
- ✅ **开场剧情接入**：story_opening.json（22 条）+ StoryPanel 播放器（新局插播、逐字、双立绘、跳过/下一句、剧情三音效）+ 导入菜单。验收：新局必播→播完进首事件、播放/跳过手感。；prefab 菜单残留已清、`Img_Character_R` 已挂 Tester_Default 立绘并注册 Addressables（2026-08-26 审计）
- ✅ **编辑界面问题①：默认模块解除锁定**：InitLockedFlags 全 false，默认模块可拖出/替换/移除/重排；被替换模块回候选库需后端候选池差集动态化（已记入后端待办）。验收：4 槽可随意更换。
- ✅ **事件小文本接入（2026-08-25 已接）**：新增 `Assets/_Project/UI/EventTexts.cs` 显示层覆盖表（来源 `Assets/test/docs/事件小文本.docx`）——能力「“牧场”的漏洞」/编辑「车间」/构筑「组装车间」标题+描述替换；docx 斜体段 → TMP `<i>` 富文本同步；能力事件「刷新次数：N」动态注入（N = 全部候选剩余刷新之和）；未登记事件回退定义原文本。**数据域（events.json/EventDefinition 资产）未动**（红线）；玩法选择事件未接入（用户定案）。验收：三事件新文本+斜体、能力刷新一次后 N-1、其余事件文本不变。
- ✅ **玩法事件·二选一面板（2026-08-25 已接）**：EventPanel 玩法分支——RuleCandidatesDrawn 驱动（面板已有场景直接重建；首发/读档走 Bootstrap 懒加载后 `ShowRuleEventFromState` 回填）；复用事件面板 + Btn_EventOption 模板，2 选项无刷新、退出按钮置灰、防重复点击锁；选择 → `Resolver.SelectRule(index)`；玩法候选抽取同步落档（OnRuleCandidatesForSave）。验收：rule 节点显示二选一、点选激活玩法并推进、退出置灰、中途存档读档回填候选。
- ✅ **事件 CG 切换（2026-08-25 已接，图+位）**：EventPanel `Img_EventArt` 按事件类型换图+换位（锚点居中，直接设 anchoredPosition/sizeDelta）——能力=event_ability@(67,0,1226,868)、玩法=event_mode@(-38,0,1228,868)、编辑=event_edit@(315,0,1376,974)、构筑=event_edit@(-433,-187,1757,1242)（**构筑复用编辑图**——4 事件 3 图，2026-08-25 用户定案）；其他回退 prefab 默认图/默认位置（含模式间切换恢复）；CG 已注册 Addressables（Art 组，地址=文件名）；新增工具「工具/事件 CG 进 Addressables」（`EventArtTools.cs`）。验收：四类事件图与位置/尺寸正确、普通事件恢复默认、CG 未加载完不闪默认图。

- ✅ **顶部进度·心电图式波次进度条（2026-08-26 已接）**：`WaveProgressBar` 自动附加 BackgroundCanvas 按 waveDefs 生成段（大/中/小/空波）；「工具/战斗美术进 Addressables」已注册 battle_progress/battle_wave_*（审计确认）；旧进度代码（RefreshTurnProgress/BuildWaveNodes 等）已移除。验收：Play 看心电图节奏与回合推进透明度。
- ✅ **战斗美术分层填充（2026-08-26 完成）**：背景/8×8棋盘/棋台/隐形3D棋盘 + 前景边框 battle_foreground 已摆入 Main.unity（Image）+ 进度/波次素材已注册 Addressables。验收：Play 整体视觉。
- ✅ **主菜单与流程入口（剩余项 2026-08-26 清完）**：剧情 prefab 菜单残留已清、Img_Character_R 拼位完成（Tester_Default 已挂+注册）；Continue/设置/续玩此前已通。验收：主菜单→剧情→事件→战斗整体走一遍。
- ✅ **LoadingPanel 接入（2026-08-26 已接）**：Bootstrap 启动常驻创建（Key="Loading"，UIPanels 已注册）+ PanelTransition 面板切换过渡（压栈渐入/渐出，多处流程 ShowWithLoading）。验收：切换过渡正常、不闪白。

## 本轮已完成（一句话总结）

- ✅ **能力事件三选一面板**：EventPanel 能力分支+Bootstrap 首发/读档回填；刷新并入长按候选，无独立按钮。
- ✅ **事件小文本接入（2026-08-25）**：EventTexts 覆盖表（docx 来源，斜体 <i> + 刷新次数动态注入）；EventPanel 三入口（普通/能力/玩法）统一走覆盖表；编译 0 错误，待人工验收。
- ✅ **编辑面板重构断链 NRE**：按嵌套 Piece_Handcard 适配带判空守卫，Play 全流程 0 错误（未改代码）。
- ✅ **牌组构筑信息区 hover 不刷新**：ResolveNodes 改 FindDeep 定位，立绘随 hover 刷新，背景不透明、空槽常显降透明。
- ✅ **所有界面退出按钮 → 确认弹窗**：事件/战斗退出统一 ConfirmPanel，确认=保存回主菜单，取消=无改动；顺带修复 Btn_Exit 解析。
- ✅ **存档系统对接后端**：EventOpened/能力候选/开战检查点/退出自动落档，GameState、RandomManager、Tutorial、Progress 四快照。
- ✅ **面板进入全量刷新布局**：PanelBase.Show() 统一 RefreshLayout（含 ContentSizeFitter），事件面板对描述区二次重排；选项就位后再刷新。
- ✅ **主菜单 Continue 对接**：读档重建会话，事件/编辑中断恢复；战斗中续玩由后端临时方案接上（回战斗开始 SL 重打）。
- ✅ **BattleController 括号修复（2026-08-24）**：`OnPhaseChanged`"阶段展示信号"块被挤出方法体致 19 编译错，已复位；编译 0 错误 0 警告。
- ✅ **主角团立绘整理（2026-08-24）**：飞书「朱-主角团」17 张立绘已下载并整理进 `Assets/Art/Characters/Protagonist/`（Tester×1 / ChessPlayer3×10 / Xeon×6），全部透明底 RGBA、Sprite 导入（maxTextureSize 2048）；已注册进 Addressables Art 组（地址 = 文件名；`Xeon_常态` 等 6 个差分地址与 StoryPanel 差分加载约定一致）；新增 Editor 工具 `Assets/_Project/Editor/Tools/PortraitTools.cs`（菜单「工具/立绘进 Addressables」——⚠️ 暂因 StoryPanel.cs 他人 WIP 编译错误不可用，本次注册已用等效 execute_code 完成）。棋手 10 张经 agnes 视觉核验按特征命名（01 Default + 02~10 特征标签）。测试员 `Tester_Default` 供李毕挂 StoryPanel `Img_Character_R`。
- ✅ **事件面板 CG 整理（2026-08-24）**：飞书「朱-cg-v1」3 张 CG 已导入 `Assets/Art/Event/`：`event_ability.png`（能力事件）/ `event_edit.png`（棋子编辑事件）/ `event_mode.png`（玩法事件），均为 3508×2480、Sprite 导入（maxTextureSize 2048，配置同 `event_bg`）；未注册 Addressables（与既有 event_* 一致，供李毕 prefab 直引）。
- ✅ 其余已完成事项（抽牌/构筑/升变预告对接、编辑候选、实时计分、战斗协议与稳定性、升变拖拽、攻击音效分发、BGM 切换、PreviewRange 修复、升变预告 buff 提示等）见 `docs/archive/前端待办-已完成事项.md`。

## 编辑界面方案沟通（问题①已定案并实施、待验收——见待验证栏；问题②③已实施并归档）

> 背景：实测编辑事件发现两个交互问题（① 默认程序模块无法替换 ② 模块可重复放置）。详见决策记录：`内部文档/决策记录/决策记录_编辑程序约束与生命周期清理_20260823.md`。

1. **问题①：默认程序模块无法替换——已定案（2026-08-24 用户：默认模块全部可编辑）+ 前端已实施（解除锁定，`InitLockedFlags` 全 false），待人工验收（见待验证栏）**；被替换/移除的默认模块回候选库需后端候选池差集动态化（已记入 `docs/后端待办.md`）。原背景：`InitLockedFlags` 曾把默认程序前 N 槽绝对锁定，4 槽填满后无法更换模块。
   - **⚠️ 范围修正 + 新配合点（2026-08-24「编辑事件取消效果编辑」定案，后端已实施）**：① 编辑事件候选从 6 改 **4**（移动 2 + 攻击 2，效果不再抽取）；② 编辑面板候选池不再含任何效果模块（后端三层过滤已做，前端无需改动候选数据）；③ **前端配合：效果槽恢复锁定**——`InitLockedFlags` 按**槽内容类型**（`slot is EffectTemplate` → locked）而非前 N 槽；拖放来源/目标/移除校验对效果槽一律拒绝（默认内置效果 = 盾兵 Effect-1 / 刺客 Effect-2 / 炮手 Effect-3，保留原位不可动）。决策记录：`内部文档/决策记录/决策记录_编辑事件取消效果编辑_20260824.md`。（2026-08-26 审计：效果槽锁定未实施——`InitLockedFlags` 仍全 false、效果槽仍可拖出/移除）
2. **问题②：模块可重复放置 → 消耗制候选（✅ 后端已实施 2026-08-23 晚，策划定案"池子构成规则"）**——候选池 = 模板库 − **本层占用增量**；放入→候选消失；撤销/移除→候选回来；进层→消耗复原；跨层可叠加。**前端零改动**（`ProgramEdited → RefreshProgramList` 刷新链现成，自动跟随候选变化）。⚠️ 请前端确认：刷新链实测 OK 即可；若对"消失"形式有偏好可再议。详见归档。
3. **问题③：后端校验兜底（已撤销，知会）**——原"同类型同 id 拒绝落账"校验已按策划定案撤销（允许跨层叠加）；重复防护由消耗制候选保证。详见归档。

## 音频挂点方案（2026-08-23 追加——后端已就绪，挂点在前端）

> 16 个 SFX 资源已导入 `Assets/Audio/SFX/`（来自音频同学交付；`sfx_death_ui` 原名含空格已改名合规，用途已确认 = **玩家死亡提示音**）。地址常量已全部建好（`TheLaw.Core.AudioRefs`）。**你在 Unity 里跑一次菜单「工具/音频进 Addressables」即完成资源注册**（地址规则 = `SFX/文件名`，与常量一一对应）。
> 播放 API：`AudioManager.Instance.PlaySFX(address, volumeScale = 1f, pitch = 1f)`（Core 单例，随处可调；缺失只 LogWarning 不报错）。

### 地址常量总表（可直接 `AudioRefs.xxx`）

| 常量 | 地址 | 用途 | 优先级 |
|---|---|---|---|
| `SfxMove` | `SFX/move` | 移动/落地（已有挂点 ✓） | P0 |
| `SfxDeploy` | `SFX/deploy` | 部署落子（已有挂点 ✓） | P0 |
| `SfxPromote` | `SFX/promote` | 升变替换/进化（同时覆盖敌方升变闪光）✅ 已接 | P0 |
| `SfxDraw` | `SFX/draw` | 抽牌（首回合抽4/1AP抽1）＋**复用：属性复制牌获得** ✅ 已接 | P0 |
| `SfxAttackMelee` | `SFX/attack_melee` | 近战（✅ 已接） | P0 |
| `SfxAttackMeleeAoe` | `SFX/attack_melee_aoe` | 近战群攻（✅ 已接） | P0 |
| `SfxAttackDirect` | `SFX/attack_direct` | 直射（✅ 已接） | P0 |
| `SfxAttackArcing` | `SFX/attack_arcing` | 抛射（✅ 已接） | P0 |
| `SfxAttackSpell` | `SFX/attack_spell` | 法术（✅ 已接） | P0 |
| `SfxHit` | `SFX/hit` | 命中/受击（含友伤；已有挂点 ✓） | P0 |
| `SfxDeath` | `SFX/death` | 死亡（已有挂点 ✓） | P0 |
| `SfxShield` | `SFX/shield` | 护盾抵挡"铛"（与掉血区分）✅ 已接 | P1 |
| `SfxFreeAction` | `SFX/free_action` | 免费行动获得/发动"叮" ✅ 已接 | P3 |
| `SfxScore` | `SFX/score` | 回合末得分结算入账（**回合末一次性，非逐击杀；第 1 关无结算**——2-4 关敌方回合结束入账时播）✅ 已接 | P3 |
| `SfxMahjongTile` | `SFX/mahjong_tile` | **麻将 M1-M8 全部 + UI/事件 U1-U9 全部**（统一复用一套）✅ 已接（UI 碰撞音，2026-08-24） | P1 |

### 待补挂点清单（按触发场景）

**战斗内**（BattleController 表现层）：
1. **攻击按方式分发 ✅ 已闭环（2026-08-23）**：`DamageInfo.AttackMode` 字段后端已就绪（8603793）+ 前端已在 `PlayDamage` 切换为 `PlaySFX(GetAttackSfx(info.AttackMode))`（1ee07de）——5 种攻击音分发完成；剩余同类挂点：升变/抽牌/护盾/免费/得分（见下 2-6 项）
2. **升变**（A3）✅：`OnPiecePromoted` 闪白/替换表现处播 `SfxPromote`。
3. **抽牌**（A4）✅：`OnHandChanged` 手牌数增长统一播 `SfxDraw`（首回合抽4/1AP抽1/复制牌获得同用）。
4. **护盾**（C1）✅：`PlayDamage` 按护盾抵挡区分播 `SfxShield`/`SfxHit`。
5. **免费行动**（C7）✅：`OnExtraActionGranted` 插入提示时播 `SfxFreeAction`。
6. **得分结算**（C10）✅：`RefreshScore` 按 `scoreDeductEnabled` 门控播 `SfxScore`（仅 2-4 关；第 1 关无）。⚠️ 口径疑点（2026-08-24）：仓库暂无 floor2-4 配置、后端文档"3/4 关启用"与前端"2-4 关"表述不一——待配置落地后人工验收。

**UI / 事件关**（面板层——**全部统一 `SfxMahjongTile`**）：
7. 按钮/面板开关/确认取消/遗物获得（ItemGettingPanel）/编辑拖放替换撤销/三选一面板/构筑加减牌/Tooltip——同一套碰撞音（音量可轻）**✅ 已接（2026-08-24，待人工验收）**：新增 `Assets/_Project/UI/Widgets/UiSfx.cs`（`UiSfx.Play` 默认音量 0.4）；覆盖 12 文件 23 处——PanelBase 开关（含恢复下层幂等守卫）、事件选项/继续/长按刷新、设置/退出、构筑加减、编辑拖放落账/回槽/撤销/完成、三选一确认、ConfirmPanel 确认/取消/背景、遗物获得确认、Tooltip 新浮窗、主菜单四按钮。未覆盖面：战斗面板按钮（属 BattleController 表现层）、滑杆/下拉（防刷屏）。防重叠（2026-08-24）：`AudioManager.PlaySFX` 新增 `restartIfPlaying` 标记——同种碰撞音在播时先停旧实例再从头重播；`UiSfx.Play` 已带此标记（连点/同帧触发盖旧声不叠音）。

**麻将玩法**（全部 `SfxMahjongTile`；可选音高随点数）：打出墙体/麻将破坏/牌山填数/刻子顺子/番数+1/摸切/雀头——`pitch` 传 `0.8 + 点数×0.08` 之类即可（AudioManager 内置 ±3% 随机）**（2026-08-26 审计：UI/事件面板已接——UiSfx 覆盖 12 文件 23 处；战斗麻将玩法 M1-M8 未接——打出/破坏/牌山/刻顺/番数/摸切/雀头）**

**属性玩法**：复制牌获得 = 复用 `SfxDraw` ✅（走 `HandAddCard→HandChanged→手牌数增长统一播`）

> ✅ 攻击按方式分发与 BGM 主菜单/战斗/事件/结算切换已完成（2026-08-23），详见 `docs/archive/前端待办-已完成事项.md`。

### 未交付 / 挂起（无需等，接完以上即可先上）

- `SFX/countdown`（末波倒计时 P3——资源未交付，可延后）
- `SFX/mahjong_hu`（和牌加重版——可做可不做，未交付）
- `SFX/ui_test`（设置面板音量试听 P2——未交付）
- ✅ **`SfxDeathUi` 挂点（2026-08-24 已接，待人工验收）**：2026-08-23 设计确认 = **玩家死亡提示音**（我方判负/玩家死亡时播放）——`BattleResultPanel` 判负表现处播 `SfxDeathUi`（默认音量）。
- ⏳ **开场剧情音效 ×3（2026-08-23 资源已交付，挂点待前端）**：`SfxStoryWallBreak`（墙壁碎裂）/ `SfxStoryScrape`（摩擦沙沙）/ `SfxStoryStatic`（雪花屏）——**开场剧情演出音效**；挂点建议：剧情面板/开场演出动画处按剧情节奏播放（`AudioRefs` 常量与资源已就绪；剧情面板接入见高优先级"主菜单与流程入口"条目）**✅ 已接（2026-08-24）**：`StoryPanel.cs` 播放器按 `cue.sfx` 播 `SfxStoryWallBreak/SfxStoryScrape/SfxStoryStatic`（关键词+常量名+地址三兼容）。

## 本轮已完成（2026-08-23）

- ✅ **知会（2026-08-24，后端改动共享 Bootstrap——无需前端配合）**：读档改保留战斗面板复用（keepBattlePanel: true），默认行为不变。
- ✅ **🔴 紧迫·能力事件三选一面板 + 每项刷新一次**：EventPanel 能力分支+Bootstrap 首发/读档回填；刷新并入长按候选，无独立按钮。
- ✅ **🔴 紧急·编辑面板重构断链 NRE**：已按嵌套 Piece_Handcard 适配带判空守卫，Play 全流程 0 错误（未改代码）。
- ✅ **牌组构筑信息区 hover 不刷新（2026-08-23 已修复）**：ResolveNodes 改 FindDeep 定位嵌套 Piece_Handcard，立绘随 hover 刷新，背景不透明、空槽常显降透明。
- ✅ **所有界面退出按钮 → 确认弹窗（2026-08-23 已完成）**：事件/战斗统一 ConfirmPanel，确认=保存回主菜单、取消=无改动；顺带修复 Btn_Exit FindDeep 兜底。
- ✅ **存档系统对接后端（2026-08-23 已完成）**：事件打开/能力候选/开战检查点/退出自动落档，含 GameState、RandomManager、Tutorial、Progress 四快照。
- ✅ **进入面板全量刷新布局（2026-08-23 已完成）**：PanelBase.Show() 统一 RefreshLayout（含 ContentSizeFitter），事件面板对描述区二次重排；选项就位后再刷新。
- ✅ **主菜单 Continue 对接（2026-08-23 部分完成，2026-08-24 续玩已接）**：读档重建会话，事件/编辑中断恢复；战斗中续玩已由后端临时方案接上（回战斗开始 SL 重打）。
  - 其余已完成事项（抽牌/构筑/升变预告对接、编辑候选、实时计分、战斗协议与稳定性、升变拖拽、攻击音效分发、BGM 切换、PreviewRange 修复、升变预告 buff 提示等）已移入 `docs/archive/前端待办-已完成事项.md`。

## 不在此文件跟踪

- 后端规则：见 `docs/后端待办.md`。
- 跨端 / 策划决策：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。
