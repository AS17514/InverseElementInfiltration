# 前端待办

> 当前唯一前端任务源。跨端 / 策划决策见 `docs/待确认.md`。

## 高优先级

- [ ] **主菜单与流程入口**：完成 Continue、设置、剧情入口；接入剧情面板；Continue 支持中断后的完整流程恢复。
- [ ] **战斗美术分层填充**：按 UI 平面方案拼接背景、8×8 棋盘、棋台景框、前景边框与顶部进度素材；保留隐形 3D 棋盘。
- [ ] **战斗场景前后层级统一排序**：统一 3D 棋子、3D TMP、SpriteRenderer、战斗面板、Tooltip、弹窗 Canvas 的 sorting layer/order；确保常驻信息不被棋盘或 UI 遮挡，弹窗可置顶点击。
- [ ] **牌库面板**：`Btn_Graveyard` 已改显示名“牌库”；点击后弹出牌库窗口，分区显示抽牌区、弃牌堆、墓地。待补牌库面板 prefab、按钮事件、弃牌堆数据与关闭/层级规则。
- [ ] **新手教程**：覆盖摆放、程序执行、抽牌、敌方波次与升变概念；升变说明为“玩家回合有 1 AP 时，将升变手牌拖到任意未升变己方棋子上”。

> ⚠️ **致歉声明（2026-08-22）**：此前"后端是否足以支持第 1 关全流程"的审查，因以"现有实现可运行性"为标准，**漏报了"能力事件尚未按策划案落地（词条过滤/三选一）"这一缺漏**，导致前端未能提前规划该面板。现后端已按策划案补齐（见下方能力事件条目），请前端按新条目开展。**再次为审查漏报致歉。**

- [ ] **能力事件三选一面板 + 每项刷新一次**（2026-08-22 后端已完成，前端待接）：
  - **策划原文**：
    > "根据玩家已选择的玩法，从能力池中随机显示三个拥有对应词条的能力供玩家选择，每个选项可各自刷新一次。"
    > "初始玩法（即偏向于下棋的玩法）的词条为基础。"
  - **机制（后端已实现）**：能力事件打开 → 按当前玩法词条过滤能力池（第 1 层 = 基础词条 basic）→ **随机 3 个候选**（`GameState.AbilityCandidates`，事件 `AbilityCandidatesDrawn` 携带）→ 三选一 + **每项可刷新一次**（刷新 = `Resolver.RefreshAbilityCandidate(index)`；刷新次数在 `GameState.AbilityRefreshLeft`）
  - **刷新抽取规则（后端已实现）**：优先从「池 − 已展示过的」抽（不会刷出已经出现过的）；池将空 → 回填（已展示的回池，但**至少不回当前被刷掉的那一项**——不会出现"刷了个寂寞"）
  - **选择** = `Resolver.SelectAbility(index)`（GrantRelic 落账 + 事件完成推进）
  - **前端需要**：三选一面板（显示 3 个候选的 `displayName/description`）+ 每项"刷新"按钮（次数用尽置灰）
  - 参考：编辑事件三选一面板（EditCandidatePanel）——能力事件为独立界面（是否复用可自行评估）
- [ ] **行动经济 buff 显示**（2026-08-22 后端已完成）：
  - **策划原文**："执行棋子的行动不消耗行动点，你场上的所有棋子一回合只能行动一次。"
  - **语义（已确认）**：玩家对该棋子一回合只能**执行一次行动**（不是"只能动一次"）；执行免费；免费行动/额外行动穿透该限制
  - 后端状态：已行动集 `GameState.ActionEconomyActed`（玩家回合开始重置）；buff 文案已备 `action_economy`（本回合可免费行动一次）/ `action_economy_acted`（已行动——不能再普通执行）
  - 前端需要：BuffDisplay 按棋子状态显示两项之一（与回合重置同步刷新）
- [ ] **插入执行（免费行动"获得即立即执行"）UI 形态（待沟通）**：
  - **策划原文**："免费攻击一次 → 必须立刻执行攻击（插入性行动）"（2026-08-19 确认——无保留语义）
  - **后端已实现（2026-08-22）**：击杀授予免费行动 → 当前一段执行收尾后**强制该棋立即执行**（free 额外行动）——当前为朴素"强制进入该棋执行选格"
  - **前端 UI 形态待定**：自动弹目标选择？强制锁定该棋子？该棋无目标时如何处理（空放语义）？——与 docs/待确认.md 免费行动执行方式同源，请前端沟通后定形态
  - 注：能力"抽到编辑牌→立即部署/升变+执行"（E5）与插入执行同链，后端预留，后续接入

## 低优先级 / 暂缓

- [ ] **谢幕面板**：首次通关后的独立 Thanks for playing / 结局报幕；不属于普通战斗结果面板。
- [ ] **LoadingPanel 接入**：预制体已注册，尚未纳入流程。
- [ ] **表现队列收尾防御**：未来表现链复杂后，销毁控制器前等待表现队列排空；当前 `ResetState` 已兜底，无已知可达故障。

## 本轮已完成

- 战斗临时协议与稳定性对接：手牌 UI 按 `Card.instanceId` 保留、复用与拖拽识别；部署/升变请求携带被操作牌的 `cardInstanceId`，失败回手按实例判定；表现队列按每组启动时快照的 `(battleSessionId, actionId)` 回执 `PresentationInfo`，AOE 组内并行全部完成后仅回执一次；新局进入爬塔前预加载 BattlePanel，避免首波部署表现事件丢失导致回执超时；部署预览在隐形棋盘 Collider 未命中时回退到棋盘平面换算并恢复合法格吸附。

- 玩家升变拖拽对接：升变牌保留在 `Grp_Hand`；玩家回合、至少 1 AP 时可拖到任意未升变己方棋子，调用 `PromoteRequest`；成功复用闪白/替换立绘与手牌刷新链，非法目标/AP 不足/非玩家回合回手；未新增节点或 prefab 改动。

- 战斗稳定性收敛：局内 Addressables 面板按会话代际丢弃旧回调；棋子视觉改战斗注册表管理；手牌 Canvas 继承根 Canvas 相机与渲染模式；Tooltip 只响应最新 hover 请求；部署预览统一检查 `IsBlocked`；受伤/升变实时刷新信息和计分。

- 编辑事件候选对接：`PieceEditPanel` 现按 `GetEditCandidates(defId)` 刷新；切换棋子和 `ProgramEdited` 后刷新候选区；show 模式下内置模块回原位先走 `TryRestoreBuiltinModule`。
- 抽牌、构筑规则与敌方升变预告前端对接已完成。
- 实时计分字段已接入 `Main/UI`：`Txt_TotalScore`、`Txt_WaveScore`、`Txt_BaseScore`、`Txt_Multiplier`、`Txt_TurnScore`。

## 不在此文件跟踪

- 后端规则：见 `docs/后端待办.md`。
- 跨端 / 策划决策：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。