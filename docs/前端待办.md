# 前端待办

> 当前唯一前端任务源。跨端 / 策划决策见 `docs/待确认.md`。
> 已完成事项归档：`docs/archive/前端待办-已完成事项.md`（本文件只保留未完成任务、前置条件与人工验收项）。

> 😩 **吐槽（2026-08-23，后端代发）**：前端待办越来越多了……感觉会好辛苦……谁把我的美好时光当海苔给吃掉了？
> 我才看到，啥时候积这么多了🥲🥲🥲

## 高优先级（未完成）

- [ ] **主菜单与流程入口**：Continue（事件/编辑中断恢复 ✓；2026-08-24 战斗中续玩已接——回战斗开始 SL 重打，后端 SL 槽 + StartBattle，详见 docs/后端待办）、设置入口 ✓；**剧情接入已完成（2026-08-24，见待验证「开场剧情接入」）——剩：剧情 prefab 菜单残留清理、`Img_Character_R` 拼位、整体人工验收**（规格：每局开始必播→播完进首事件、任意键/点击下一句、长按 0.8s 跳过、逐字打字、双立绘位 L=Xeon/R=测试员、剧情三音效；立绘素材已入库见已完成「主角团立绘整理」）。
- [ ] **战斗美术分层填充**：背景、8×8 棋盘、棋台景框与隐形 3D 棋盘已装配 ✓；**待补：前景边框与顶部进度素材**（battle_foreground / battle_progress 仅 meta 引用，未接入场景）。
- [ ] **牌库面板**（`Btn_Graveyard` 已改显示名"牌库"并解析引用）：弹出窗口分区显示抽牌区、弃牌堆、墓地。**待补：面板本体、点击事件、关闭/层级规则**。后端数据源见下。
  - 后端数据源已就绪（只读；**面板打开读一次快照即可**，实时同步监听现有事件即可，无需新事件）：**弃牌堆 = 墓地** = `GameState.Graveyard`（玩家棋子死亡 defId=升变后形态）；另有 `PromotedReplacedPile`（升变替换池）、`MahjongUsedPile`/`MahjongDeadPile`（麻将使用/死亡池）、`DiscardZone.PieceDeaths/MahjongDeaths`（弃牌区卡片化记录）——变化事件 `PieceDied`/`PiecePromoted`/`StateChanged("mahjong-wall")`。
- [ ] **新手教程**：覆盖摆放、程序执行、抽牌、敌方波次与升变概念；升变说明为"玩家回合有 1 AP 时，将升变手牌拖到任意未升变己方棋子上"。
- [ ] **新玩法前端接入**（2026-08-24 后端代码就绪——骰子/围棋/代币，仅玩家侧；**2026-08-24 审计：前端三件 UI 均未接**；**激活入口 = 玩法事件二选一（2026-08-24 定案——见下方「玩法事件·二选一面板」条目；此前"每关固定玩法"记录已作废）**）：
  - **骰子**（`GameState.DiceValue`）：投掷按钮（执行类行动 1 AP，发 `RollDiceRequest`）；"点数直线移动"启动按钮（发 `DiceMoveRequest`，不耗 AP 消耗点数）→ `StateChanged("dice-move-select")` → 弹方向选择（上/下/左/右，点数步直线）→ `BattleFlow.OnDiceDirectionSelected(Direction)`；buff 提示不跨回合。
  - **围棋**：部署按钮（发 `DeployGoRequest(cell)`——不耗 AP、每回合 1 次、任意空格）；退场复用 `PieceDied` 表现。
  - **代币**（`GameState.TokenCount`）：购买面板（发 `BuyTokenRequest(discardIndex)`——读弃牌区 `DiscardZone.PieceDeaths/MahjongDeaths` 选牌；显示牌价值与所需代币；复制入手牌）。
  - 请求无需新传参（只带 cell/discardIndex）；新状态全部公开可读。

- [ ] **玩法事件·二选一面板**（2026-08-24 后端已实施——策划定案：随机抽 2 二选一、不可刷新；**旧"每关固定玩法"记录已作废**）：监听 `RuleCandidatesDrawn`（携带 `GameState.RuleCandidates`——2 个玩法 id）；复用能力事件三选一面板形态（选项两个、**无刷新按钮**）：显示玩法名（麻将/属性/骰子/围棋/代币）+ 简述；点选 → 后端 `Resolver.SelectRule(index)` 落账（激活 + 事件完成推进）；事件退出按钮置灰（与能力事件同）。⚠️ 前端面板就绪前**第 2-4 关不得配置 rule 节点**（能力事件无面板卡死教训）。

- [ ] **能力交互面板 ×2（2026-08-24 后端已就绪，能力池 P1）**：① **宝牌选数**——获得「宝牌」能力后（RelicObtained，或三选一落账后）弹 **1-9 数字选择面板** → `Resolver.SetBaopaiNumber(n)`（后端校验持有+1-9）；② **出千选数**——投掷时收到 `StateChanged("dice-rig-select")` → 弹 **1-6 自选面板** → `BattleFlow.OnDiceNumberSelected(n)`；**不跨回合**（未选则作废，下一回合消失）。面板形态可复用能力事件候选样式（简单按钮网格）。
  - **策划原文**（口述文档：`内部文档/方案与配置器/能力池完整清单_策划口述_20260824.md`）：宝牌 = "游戏开始时，选择一个1—9的数字，本局游戏中那个数字对应价值的牌将被视为'宝牌'，'宝牌'进入弃牌区时基础得分＋其价值数，参与凑成刻子或顺子时番数额外＋1"；出千 = "行动点+1 投掷骰子的点数可以自选"。
  - ⚠️ 两个面板就绪前，对应能力照常可被选中（宝牌/出千入口缺面板 = 能力"有勾选但无生效交互"——宝牌会一直等选数、出千会一直等自选；**面板未接期间建议先不把这两条加入候选池**或前端按缺省值兜底（宝牌默认随机数字 / 出千默认随机投掷），具体取舍前端定——后端无阻塞）。

- [ ] **震击墙表现（2026-08-24 后端已就绪，能力池 P3）**：`StateChanged("shock-walls")` 事件（开战生成时发）→ 在棋盘渲染 2 个不可破坏墙（区别于麻将墙：不可破坏 + 攻击溅射）；攻击溅射伤害走既有 `DamageDealt` 逐目标飘字（Damage=1、FriendlyFire=true——与 AOE 同款，复用现有表现即覆盖）。后端状态读取：`GameState.ShockWalls`（格子集合，战斗级）。

**回合结束手动化（2026-08-23 后端已实施，前端零改动）**：AP 耗尽不再自动结束回合——玩家手动点"结束回合"（无需改）；可选增强（ap-empty 提示）已实施，见待验证栏。

## 低优先级 / 暂缓

- [ ] **弃牌区 / 抽牌堆点击查看**（2026-08-24 提议——**待与策划讨论**，先放着不动；后端数据已就绪）。
- [ ] **谢幕面板**：首次通关后的独立 Thanks for playing / 结局报幕；不属于普通战斗结果面板。
- [ ] **LoadingPanel 接入**：预制体已注册，尚未纳入流程。
- [ ] **表现队列收尾防御**：未来表现链复杂后，销毁控制器前等待表现队列排空；当前 `ResetState` 已兜底，无已知可达故障。

## 待验证（已完成·待人工验收）

> 已完成（代码级证据/已实施）但尚未人工验收（Play 实测）的事项集中于此；验收通过后移回「本轮已完成」并归档。

- ✅ **战斗场景前后层级统一排序**：棋子按行排序、阴影最底、手牌50/列表60/Tooltip1000 分层排序已代码配置。验收：常驻信息不被遮挡、弹窗可置顶。
- ✅ **行动经济 buff 显示**：BuffDisplay 按棋子输出 action_economy/action_economy_acted（ActionEconomyBuffSync 订阅既有事件补发刷新）。验收：获能力后显示态 A，执行后态 B，新回合回态 A。
- ✅ **插入执行（免费行动）UI 形态**：提示条+自动锁定棋子+弹目标选择+允许空放（TryStartForcedExec/ShowFreeActionTip）。验收：击杀触发→提示、锁定、目标选择、空放、自动收尾。
- ✅ **回合结束手动化可选增强（ap-empty 提示）**：BattleController 消费 StateChanged("ap-empty")，结束回合按钮 Tooltip 提示（1.6s 自动消失）。
- ✅ **通用变亮通道接入**：监听 HintRequested；CardQualify 手牌变亮+能力面板金色脉冲，按 EditedCardQualifyId 回填。
- ✅ **能力事件显示修订**：描述区去掉候选清单；遗物图标改代码色块；能力栏兼容回退 displayName。
- ✅ **战斗音效补挂**：玩家判负 SfxDeathUi（BattleResultPanel）+ promote/draw/shield/free/score 复核在位（音频挂点清单 2-6 条）。
- ✅ **UI/事件面板碰撞音**：UiSfx.Play 统一碰撞音覆盖 12 文件 23 处（音频挂点清单第 7 条）。
- ✅ **开场剧情接入**：story_opening.json（22 条）+ StoryPanel 播放器（新局插播、逐字、双立绘、跳过/下一句、剧情三音效）+ 导入菜单。验收：新局必播→播完进首事件、播放/跳过手感。
- ✅ **编辑界面问题①：默认模块解除锁定**：InitLockedFlags 全 false，默认模块可拖出/替换/移除/重排；被替换模块回候选库需后端候选池差集动态化（已记入后端待办）。验收：4 槽可随意更换。

## 本轮已完成（一句话总结）

- ✅ **能力事件三选一面板**：EventPanel 能力分支+Bootstrap 首发/读档回填；刷新并入长按候选，无独立按钮。
- ✅ **编辑面板重构断链 NRE**：按嵌套 Piece_Handcard 适配带判空守卫，Play 全流程 0 错误（未改代码）。
- ✅ **牌组构筑信息区 hover 不刷新**：ResolveNodes 改 FindDeep 定位，立绘随 hover 刷新，背景不透明、空槽常显降透明。
- ✅ **所有界面退出按钮 → 确认弹窗**：事件/战斗退出统一 ConfirmPanel，确认=保存回主菜单，取消=无改动；顺带修复 Btn_Exit 解析。
- ✅ **存档系统对接后端**：EventOpened/能力候选/开战检查点/退出自动落档，GameState、RandomManager、Tutorial、Progress 四快照。
- ✅ **面板进入全量刷新布局**：PanelBase.Show() 统一 RefreshLayout（含 ContentSizeFitter），事件面板对描述区二次重排；选项就位后再刷新。
- ✅ **主菜单 Continue 对接**：读档重建会话，事件/编辑中断恢复；战斗中续玩由后端临时方案接上（回战斗开始 SL 重打）。
- ✅ **BattleController 括号修复（2026-08-24）**：`OnPhaseChanged`"阶段展示信号"块被挤出方法体致 19 编译错，已复位；编译 0 错误 0 警告。
- ✅ **主角团立绘整理（2026-08-24）**：飞书「朱-主角团」17 张立绘已下载并整理进 `Assets/Art/Characters/Protagonist/`（Tester×1 / ChessPlayer3×10 / Xeon×6），全部透明底 RGBA、Sprite 导入（maxTextureSize 2048）；已注册进 Addressables Art 组（地址 = 文件名；`Xeon_常态` 等 6 个差分地址与 StoryPanel 差分加载约定一致）；新增 Editor 工具 `Assets/_Project/Editor/Tools/PortraitTools.cs`（菜单「工具/立绘进 Addressables」——⚠️ 暂因 StoryPanel.cs 他人 WIP 编译错误不可用，本次注册已用等效 execute_code 完成）。棋手 10 张经 agnes 视觉核验按特征命名（01 Default + 02~10 特征标签）。测试员 `Tester_Default` 供李毕挂 StoryPanel `Img_Character_R`。
- ⚠️ **编译观察（2026-08-24，他人 WIP，非本次改动）**：`Assets/_Project/UI/Views/StoryPanel.cs`（未跟踪文件）3 处编译错误：`StoryCue` 缺 `type` / `showXeon` / `showTester` 字段（CS1061×3，行 437/441/442）——疑剧情面板接入进行中，未处理，待其所有者修复。
- ✅ 其余已完成事项（抽牌/构筑/升变预告对接、编辑候选、实时计分、战斗协议与稳定性、升变拖拽、攻击音效分发、BGM 切换、PreviewRange 修复、升变预告 buff 提示等）见 `docs/archive/前端待办-已完成事项.md`。

## 编辑界面方案沟通（问题①已定案并实施、待验收——见待验证栏；问题②③已实施并归档）

> 背景：实测编辑事件发现两个交互问题（① 默认程序模块无法替换 ② 模块可重复放置）。详见决策记录：`内部文档/决策记录/决策记录_编辑程序约束与生命周期清理_20260823.md`。

1. **问题①：默认程序模块无法替换——已定案（2026-08-24 用户：默认模块全部可编辑）+ 前端已实施（解除锁定，`InitLockedFlags` 全 false），待人工验收（见待验证栏）**；被替换/移除的默认模块回候选库需后端候选池差集动态化（已记入 `docs/后端待办.md`）。原背景：`InitLockedFlags` 曾把默认程序前 N 槽绝对锁定，4 槽填满后无法更换模块。
   - **⚠️ 范围修正 + 新配合点（2026-08-24「编辑事件取消效果编辑」定案，后端已实施）**：① 编辑事件候选从 6 改 **4**（移动 2 + 攻击 2，效果不再抽取）；② 编辑面板候选池不再含任何效果模块（后端三层过滤已做，前端无需改动候选数据）；③ **前端配合：效果槽恢复锁定**——`InitLockedFlags` 按**槽内容类型**（`slot is EffectTemplate` → locked）而非前 N 槽；拖放来源/目标/移除校验对效果槽一律拒绝（默认内置效果 = 盾兵 Effect-1 / 刺客 Effect-2 / 炮手 Effect-3，保留原位不可动）。决策记录：`内部文档/决策记录/决策记录_编辑事件取消效果编辑_20260824.md`。
2. **问题②：模块可重复放置 → 消耗制候选（✅ 后端已实施 2026-08-23 晚，策划定案"池子构成规则"）**——候选池 = 模板库 − **本层占用增量**；放入→候选消失；撤销/移除→候选回来；进层→消耗复原；跨层可叠加。**前端零改动**（`ProgramEdited → RefreshProgramList` 刷新链现成，自动跟随候选变化）。⚠️ 请前端确认：刷新链实测 OK 即可；若对"消失"形式有偏好可再议。详见归档。
3. **问题③：后端校验兜底（已撤销，知会）**——原"同类型同 id 拒绝落账"校验已按策划定案撤销（允许跨层叠加）；重复防护由消耗制候选保证。详见归档。

## 音频挂点方案（2026-08-23 追加——后端已就绪，挂点在前端）

> 16 个 SFX 资源已导入 `Assets/Audio/SFX/`（来自音频同学交付；`sfx_death_ui` 原名含空格已改名合规，用途已确认 = **玩家死亡提示音**）。地址常量已全部建好（`TheLaw.Core.AudioRefs`）。**你在 Unity 里跑一次菜单「工具/音频进 Addressables」即完成资源注册**（地址规则 = `SFX/文件名`，与常量一一对应）。
> 播放 API：`AudioManager.Instance.PlaySFX(address, volumeScale = 1f, pitch = 1f)`（Core 单例，随处可调；缺失只 LogWarning 不报错）。

### 地址常量总表（可直接 `AudioRefs.xxx`）

| 常量 | 地址 | 用途 | 优先级 |
|---|---|---|---|
| `SfxMove` | `SFX/move` | 移动/落地（已有挂点 ✓） | P0 |
| `SfxDeploy` | `SFX/deploy` | 部署落子（已有挂点 ✓） | P0 |
| `SfxPromote` | `SFX/promote` | 升变替换/进化（同时覆盖敌方升变闪光）✅ 已接 | P0 |
| `SfxDraw` | `SFX/draw` | 抽牌（首回合抽4/1AP抽1）＋**复用：属性复制牌获得** ✅ 已接 | P0 |
| `SfxAttackMelee` | `SFX/attack_melee` | 近战（✅ 已接） | P0 |
| `SfxAttackMeleeAoe` | `SFX/attack_melee_aoe` | 近战群攻（✅ 已接） | P0 |
| `SfxAttackDirect` | `SFX/attack_direct` | 直射（✅ 已接） | P0 |
| `SfxAttackArcing` | `SFX/attack_arcing` | 抛射（✅ 已接） | P0 |
| `SfxAttackSpell` | `SFX/attack_spell` | 法术（✅ 已接） | P0 |
| `SfxHit` | `SFX/hit` | 命中/受击（含友伤；已有挂点 ✓） | P0 |
| `SfxDeath` | `SFX/death` | 死亡（已有挂点 ✓） | P0 |
| `SfxShield` | `SFX/shield` | 护盾抵挡"铛"（与掉血区分）✅ 已接 | P1 |
| `SfxFreeAction` | `SFX/free_action` | 免费行动获得/发动"叮" ✅ 已接 | P3 |
| `SfxScore` | `SFX/score` | 回合末得分结算入账（**回合末一次性，非逐击杀；第 1 关无结算**——2-4 关敌方回合结束入账时播）✅ 已接 | P3 |
| `SfxMahjongTile` | `SFX/mahjong_tile` | **麻将 M1-M8 全部 + UI/事件 U1-U9 全部**（统一复用一套）✅ 已接（UI 碰撞音，2026-08-24） | P1 |

### 待补挂点清单（按触发场景）

**战斗内**（BattleController 表现层）：
1. **攻击按方式分发 ✅ 已闭环（2026-08-23）**：`DamageInfo.AttackMode` 字段后端已就绪（8603793）+ 前端已在 `PlayDamage` 切换为 `PlaySFX(GetAttackSfx(info.AttackMode))`（1ee07de）——5 种攻击音分发完成；剩余同类挂点：升变/抽牌/护盾/免费/得分（见下 2-6 项）
2. **升变**（A3）✅：`OnPiecePromoted` 闪白/替换表现处播 `SfxPromote`。
3. **抽牌**（A4）✅：`OnHandChanged` 手牌数增长统一播 `SfxDraw`（首回合抽4/1AP抽1/复制牌获得同用）。
4. **护盾**（C1）✅：`PlayDamage` 按护盾抵挡区分播 `SfxShield`/`SfxHit`。
5. **免费行动**（C7）✅：`OnExtraActionGranted` 插入提示时播 `SfxFreeAction`。
6. **得分结算**（C10）✅：`RefreshScore` 按 `scoreDeductEnabled` 门控播 `SfxScore`（仅 2-4 关；第 1 关无）。⚠️ 口径疑点（2026-08-24）：仓库暂无 floor2-4 配置、后端文档"3/4 关启用"与前端"2-4 关"表述不一——待配置落地后人工验收。

**UI / 事件关**（面板层——**全部统一 `SfxMahjongTile`**）：
7. 按钮/面板开关/确认取消/遗物获得（ItemGettingPanel）/编辑拖放替换撤销/三选一面板/构筑加减牌/Tooltip——同一套碰撞音（音量可轻）**✅ 已接（2026-08-24，待人工验收）**：新增 `Assets/_Project/UI/Widgets/UiSfx.cs`（`UiSfx.Play` 默认音量 0.4）；覆盖 12 文件 23 处——PanelBase 开关（含恢复下层幂等守卫）、事件选项/继续/长按刷新、设置/退出、构筑加减、编辑拖放落账/回槽/撤销/完成、三选一确认、ConfirmPanel 确认/取消/背景、遗物获得确认、Tooltip 新浮窗、主菜单四按钮。未覆盖面：战斗面板按钮（属 BattleController 表现层）、滑杆/下拉（防刷屏）。

**麻将玩法**（全部 `SfxMahjongTile`；可选音高随点数）：打出墙体/麻将破坏/牌山填数/刻子顺子/番数+1/摸切/雀头——`pitch` 传 `0.8 + 点数×0.08` 之类即可（AudioManager 内置 ±3% 随机）**（2026-08-24 审计：未接）**

**属性玩法**：复制牌获得 = 复用 `SfxDraw` ✅（走 `HandAddCard→HandChanged→手牌数增长统一播`）

> ✅ 攻击按方式分发与 BGM 主菜单/战斗/事件/结算切换已完成（2026-08-23），详见 `docs/archive/前端待办-已完成事项.md`。

### 未交付 / 挂起（无需等，接完以上即可先上）

- `SFX/countdown`（末波倒计时 P3——资源未交付，可延后）
- `SFX/mahjong_hu`（和牌加重版——可做可不做，未交付）
- `SFX/ui_test`（设置面板音量试听 P2——未交付）
- ✅ **`SfxDeathUi` 挂点（2026-08-24 已接，待人工验收）**：2026-08-23 设计确认 = **玩家死亡提示音**（我方判负/玩家死亡时播放）——`BattleResultPanel` 判负表现处播 `SfxDeathUi`（默认音量）。
- ⏳ **开场剧情音效 ×3（2026-08-23 资源已交付，挂点待前端）**：`SfxStoryWallBreak`（墙壁碎裂）/ `SfxStoryScrape`（摩擦沙沙）/ `SfxStoryStatic`（雪花屏）——**开场剧情演出音效**；挂点建议：剧情面板/开场演出动画处按剧情节奏播放（`AudioRefs` 常量与资源已就绪；剧情面板接入见高优先级"主菜单与流程入口"条目）**✅ 已接（2026-08-24）**：`StoryPanel.cs` 播放器按 `cue.sfx` 播 `SfxStoryWallBreak/SfxStoryScrape/SfxStoryStatic`（关键词+常量名+地址三兼容）。

## 本轮已完成（2026-08-23）

- ✅ **知会（2026-08-24，后端改动共享 Bootstrap——无需前端配合）**：读档改保留战斗面板复用（keepBattlePanel: true），默认行为不变。
- ✅ **🔴 紧迫·能力事件三选一面板 + 每项刷新一次**：EventPanel 能力分支+Bootstrap 首发/读档回填；刷新并入长按候选，无独立按钮。
- ✅ **🔴 紧急·编辑面板重构断链 NRE**：已按嵌套 Piece_Handcard 适配带判空守卫，Play 全流程 0 错误（未改代码）。
- ✅ **牌组构筑信息区 hover 不刷新（2026-08-23 已修复）**：ResolveNodes 改 FindDeep 定位嵌套 Piece_Handcard，立绘随 hover 刷新，背景不透明、空槽常显降透明。
- ✅ **所有界面退出按钮 → 确认弹窗（2026-08-23 已完成）**：事件/战斗统一 ConfirmPanel，确认=保存回主菜单、取消=无改动；顺带修复 Btn_Exit FindDeep 兜底。
- ✅ **存档系统对接后端（2026-08-23 已完成）**：事件打开/能力候选/开战检查点/退出自动落档，含 GameState、RandomManager、Tutorial、Progress 四快照。
- ✅ **进入面板全量刷新布局（2026-08-23 已完成）**：PanelBase.Show() 统一 RefreshLayout（含 ContentSizeFitter），事件面板对描述区二次重排；选项就位后再刷新。
- ✅ **主菜单 Continue 对接（2026-08-23 部分完成，2026-08-24 续玩已接）**：读档重建会话，事件/编辑中断恢复；战斗中续玩已由后端临时方案接上（回战斗开始 SL 重打）。
  - 其余已完成事项（抽牌/构筑/升变预告对接、编辑候选、实时计分、战斗协议与稳定性、升变拖拽、攻击音效分发、BGM 切换、PreviewRange 修复、升变预告 buff 提示等）已移入 `docs/archive/前端待办-已完成事项.md`。

## 不在此文件跟踪

- 后端规则：见 `docs/后端待办.md`。
- 跨端 / 策划决策：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。
