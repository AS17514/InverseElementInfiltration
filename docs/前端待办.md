> 更新（2026-08-12）：删除已完成项（牌组构筑面板 / 战斗面板buff栏 / 战斗面板阶段显示 / 战斗胜利面板）；新增"敌方升变预告展示"。

## 待办

- 设置面板
- 开始面板-存档和设置，顺便还得把剧情面板搞了
- 存档面板
- 战斗面板的遗物栏
- **敌方升变预告展示**（2026-08-12 加入）：PromoteAnnounced 事件已触发但 UI 无展示——"敌方即将升变"提示（如波次预告区/战斗顶部警告），策划未定方案，先记录
- 谢幕面板
- 确认面板
- **【后端排查 2026-08-12】战斗面板阶段按钮隐藏后不恢复（做 2-4 层前必须）**：RefreshPhaseButton 的 GameOver 分支 `btn.gameObject.SetActive(false)`，Placement/PlayerTurn 分支没有 `SetActive(true)`——中途胜利（非末层）复用旧控制器时，下一场战斗摆放阶段无"结束准备"按钮 → 规则层无限等 PlacementFinished 软锁。修法：Placement/PlayerTurn 分支补 `btn.gameObject.SetActive(true)`（约 2 行）
- **【后端排查 2026-08-12】旧控制器楼层脏数据（与上条同根，一起修）**：`RefreshTurnProgress` 的 `_floor` 只在 `_floor == null` 时加载一次——中途胜利复用控制器后，新楼层战斗的波次节点/进度条沿用旧楼层数据。修法：PhaseChanged(Placement) 时重置 `_floor/_waveDefs/_waveNodes/_lastTurnCount` 并重建节点
- **【后端排查 2026-08-12】结算面板同帧按键泄漏（体验，可缓）**：战斗结束的那次点击（鼠标按下帧）→ 结算面板同帧弹出并开始检测按键 → `wasPressedThisFrame` 读到该次点击 → 立即确认，结算面板闪 0 帧就关闭（玩家看不清胜负/得分）。修法：PushOverlay 后先跳一帧再开始按键轮询（约 2 行）

## 已完成（历史记录，2026-08-12 清理）

- ✅ 牌组构筑面板（DeckBuildPanel，2026-08-11）
- ✅ 战斗面板buff栏（BuffDescTable + Txt_Other，2026-08-11）
- ✅ 战斗面板阶段显示（Txt_EventName）
- ✅ 战斗胜利面板（BattleResultPanel，2026-08-11）
- ✅ 事件关事件文案（EventDefinition title/description，2026-08-09）
- ✅ 程序块描述表（SlotDescTable + slot-descriptions.json，2026-08-11）
- ✅ 战斗顶部回合进度条（Sld_TurnProgress + 波次节点，2026-08-12）
