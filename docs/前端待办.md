# 前端待办

> 当前唯一前端任务源。跨端 / 策划决策见 `docs/待确认.md`。
> 已完成事项归档：`docs/archive/前端待办-已完成事项.md`（本文件只保留未完成任务、前置条件与人工验收项）。

> 😩 **吐槽（2026-08-23，后端代发）**：前端待办越来越多了……感觉会好辛苦……谁把我的美好时光当海苔给吃掉了？

> 我才看到，啥时候积这么多了🥲🥲🥲

## 高优先级（未完成）
**回合结束手动化（2026-08-23 后端已实施，前端零改动）**：AP 耗尽不再自动结束回合——玩家手动点"结束回合"（无需改）；可选增强（ap-empty 提示）已实施，见待验证栏。

## 低优先级 / 暂缓

- ✅ **教程播放策略·二选一定案（2026-08-26 用户拍板选 B）**——两种互斥方案：
  - **方案 A：教程只放一次（每存档/跨存档只播放一次，设备级持久 `tutorial.json`）** + 设置面板提供「重置新手教程」按钮 → 调 `TutorialSystem.ClearAll()` 后可重新播放
  - **方案 B：每次游玩都播放新手教程**，不提供重置按钮
  - ✅ **当前已选 & 保持 = B**（用户："每一次游玩都会播放……可以关掉也可以保留，我就保留这个样子也没问题"）——**无需做重置按钮**。
  - 备注：方案 A 的后端设施（`tutorial.json` 持久化 + `TutorialSystem.ClearAll()`）均已就绪，日后若要切回 A（关掉"每次播放"改为只放一次 + 加重置按钮），后端零改动、前端加按钮即可。

- [ ] **ESC 关闭面板（2026-08-25 建议）**：当前无面板支持 ESC 关闭；牌库面板等弹窗后续统一（先按面板外点击关闭）。
- [ ] **谢幕面板**：首次通关后的独立 Thanks for playing / 结局报幕；不属于普通战斗结果面板。
- [ ] **表现队列收尾防御**：未来表现链复杂后，销毁控制器前等待表现队列排空；当前 `ResetState` 已兜底，无已知可达故障。

## 待验证（已完成·待人工验收）

> 已完成（代码级证据/已实施）但尚未人工验收（Play 实测）的事项集中于此；验收通过后移回「本轮已完成」并归档。
- ✅ **能力交互面板 ×2（2026-08-27 已实现，待人工验收）**：新增 `SelectNumberPanel`（Key="SelectNumber"，Bootstrap 常驻注册 + RelicObtained 全局监听）——**宝牌选数**（获得含 Baopai 效果遗物 → 弹 1-9 → `Resolver.SetBaopaiNumber(n)`）；**出千选数**（`StateChanged("dice-rig-select")` → 弹 1-6 → `BattleFlow.OnDiceNumberSelected(n)`）。`Grp_Numbers` 容器 + `Btn_Number` 预制体运行时生成（1..max 个，写数字+接回调；Txt_Info 说明；点背景关闭）；不跨回合（回合切换自动关=作废）。SelectNumberPanel/Btn_Number 已注册 UIPanels Addressables。编译 0 错误。验收：拿宝牌能力弹 1-9 选数→该数字牌进弃牌区/组刻顺触发宝牌效果；投掷持出千弹 1-6 自选点数。
- ✅ **麻将玩法前端（2026-08-27 已实现，待人工验收）**：① `Grp_FloorPlay_Mahjong` 面板绑定——牌山 1/2 号位运行时生成 Piece_Handcard 卡（锚定占位实例位置/缩放，空牌山隐藏卡、`Txt_MahjongScore_1/_2` 同步显示数字）、`Txt_MahjongFan`=FanCount、`Btn_Hu` 和牌（⚠️ 按钮节点待李毕补，代码判空兜底）、hover 仅放大 2 倍不上浮；② 战斗手牌显示麻将卡（复用卡模板：名字=麻将/价值=点数/类型=麻，棋子牌后按点数排序）；③ 交互——点击卡=摸切（MochiRequest）、拖拽到棋盘=打墙（PlayMahjongRequest，1×2 竖两格，非法格回手）；④ 场墙占位灰块视觉（mahjong-wall/mahjong-score 刷新）；⑤ 战斗音（打出/牌山[音高随点数]/和牌，SfxMahjongTile）。数据源 MahjongScore/FanCount/MahjongWalls；刷新信号 mahjong-score/mahjong-wall/mahjong-hu。编译 0 错误。验收：选到麻将玩法 → 手牌出现麻将牌、点牌摸切、拖牌打墙、牌山两张卡随填数出现/刻顺清空、番数/和牌按钮联动。
- ✅ **玩法面板槽位填充修复（2026-08-27 已修复，待人工验收）**：`BattleController.PlayPanelPrefabSuffix` 对 mahjong/dice/go/token 首字母大写（Mahjong/Dice/Go/Token）——与 Addressables 注册地址 `Grp_FloorPlay_*`（UIPanels 组，实测已注册大写）对齐，修复选择玩法后槽位加载失败/InvalidKeyException。编译 0 错误。验收：第 2-4 层玩法选择后对应槽位正常填充（需先跑「工具/导入关卡配置（JSON）」+「收集全部配置到 Bootstrap」注册第 2-4 层——当前只有 Floor_floor1.asset）。
- ✅ **测试员反馈·事件面板设置/退出失效排查（2026-08-26 已排查，待人工验收）**：接线本身正确——运行时验证设置点击能打开 SettingsPanel；退出按钮已接线、能力事件置灰为既有设计（普通事件恢复可点）。**根因 = 并行会话未提交的 ContentReady/EndWhenReady 过渡改造（PanelBase/PanelTransition/EventPanel WIP）存在 Loading 泄漏**：事件打开后 LoadingPanel 仍 active（alpha=1、raycast=true）滞留，其叠到最上层时整块面板不可点（设置/退出/选项全失效），叠在下面时功能正常（本机复现为后者，故单测通过）。⚠️ 该泄漏文件均为并行会话 WIP，前端未改动（红线）；修复方向建议：EndWhenReady 必须保证 End 必然执行（不依赖可能被 Kill 的 DOVirtual.DelayedCall）、PushOverlay 同 key 去重、TutorialPanel 等异步面板补 ContentReady 通知。
- ✅ **测试员反馈·手牌布局 NRE（2026-08-26 已修复，待人工验收）**：`HandLayoutController.ApplyLayout` 第 161 行 `_root.rect` 空引用 → `LoadAndBuildHand` 协程中断。根因 = 战斗面板**隐藏中**（ShowWithLoading 尚未 ShowPanel）完成手牌构建 → `AddComponent<HandLayoutController>` 在 inactive 对象上 → Awake 被推迟（`_root` 未赋值）→ 首次 `RefreshCards→ApplyLayout` 空引用（加载快时必现，是我修好 RebuildHand 时序后暴露的竞态）。修复（仅 `HandLayoutController.cs`）：`RefreshCards` 兜底补 `_root` 初始化 + `ApplyLayout` 空引用守卫（面板激活后 Awake 补跑、Update 自动排布）。Play 验证：隐藏态构建不再 NRE、激活后 `hlcRoot=set`、进战斗手牌 4 张正常布局、0 Console 错误。
- ✅ **测试员反馈·第一关战斗卡死修复（2026-08-26 已修复，待人工验收）**：根因 = BattlePanel 节点解析只在 OnShow（面板 Show 时），而预加载流程下 BattleController.Init（RefreshAll/RebuildHand + 按钮接线）先于 Show 执行 → Init 时 HandRoot/按钮引用全 null → **手牌有数据但 UI 空（不发牌）** + **抽牌/结束准备按钮未接线（点击无反应）** → 第一关无法推进。修复：① BattlePanel.Awake 即 ResolveNodes（预加载实例化即解析，按钮/手牌引用就绪）；② BattleController.RebuildHand 防御——HandRoot 未就绪时等待就绪后重建（3s 超时防死等）。Play 验证：进战斗手牌 4 张正常渲染、抽牌/结束准备按钮已接线、RefreshPhaseButton 正常输出、0 Console 错误。验收：新局进战斗看到手牌，抽牌/部署/结束准备可点可玩。另：右侧「元素玩法图标」= 无玩法激活时的 3 个「未加载」占位（与验收项一致，设计如此，非 bug）。
- ✅ **测试员反馈·编辑界面白模闪帧（2026-08-26 已修复，待人工验收）**：EditCandidatePanel prefab 内候选卡默认激活（未绑定白卡）——实例化到绑定之间露「白模」闪帧；Awake 即隐藏候选卡，OnShow 绑定数据后再按候选数量激活。Play 验证：候选面板打开时卡片已绑定（无白卡）。验收：进入编辑候选界面不再先闪白卡。
- ✅ **测试员反馈·开场→事件衔接主界面闪现（2026-08-26 未复现）**：按当前代码（剧情结束 → loading 全程覆盖 → 事件面板 busy 分支接管）Play 复跑未复现主菜单闪现；MainMenu 已在剧情播放前隐藏。若仍复现请提供录屏/复现条件（是否跳过剧情、跳过时机）。
- ✅ **测试员反馈·编队（构筑）双触发/双推进跳关修复（2026-08-26 已修复，待人工验收）**：症状 = 退出棋子编辑后编队界面触发两次且布局数据异常，再次点击直接进棋盘且失去操作响应。根因链 = 重复 StateChanged("deck")/重复确认 → 双 EventCompleted → TowerFlow 双推进（后端守卫只比对 CurrentEventId，战斗开始后该 id 仍残留 → 二次信号被接受 → 跳关/整局提前结束）。修复（前端两层防御）：① Bootstrap.OpenDeckBuild 防重——加载中/已显示时忽略重复 deck 信号（防双实例 + 重复 OnShow 重建出布局异常）；② DeckBuildPanel.OnConfirm 落账成功即锁（_confirmed，OnShow 复位）——重复确认不再发第二次 EventCompleted。Play 验证：单点流程单触发；模拟双击确认 → 单次推进、战斗正常开、无整局提前结束。⚠️ 双触发的确切输入动作未复现（单点实测干净）——已按「任意来源重复信号」加固；若仍复现请提供录屏。
- ✅ **测试用自动过关（2026-08-26 已实现，待人工验收）**：战斗内按住 Ctrl + 左键连点右上设置按钮 10 次 → 强制玩家胜利推进下一关（不打开设置；前端调 BattleFlow.EndBattle 公开收尾，不直写状态）。每场战斗计数重置。
- ✅ **玩法详情按钮 + Grp_Mode 接入（2026-08-26 已实现，待人工验收）**：`FloorPlayDetailePanel`（新面板，参考 ItemGetting——Txt_Name/Txt_Info/Btn_Confirm，确认/点背景关闭）+ Bootstrap 常驻注册；BattleController `RefreshFloorMode`：Grp_Introduction 3 按钮按激活玩法（StyleRegistry.All 固定顺序，上限 3）填玩法名可点、未加载禁用+"未加载"；Grp_Play 槽位重建 = 激活玩法面板（Grp_FloorPlay_* Addressables 实例，Element→WuXing）+ 未加载用 Grp_Play_None 占位；选中棋子整组隐藏 Grp_Mode、取消恢复（复用 RefreshGrpPlayVisibility 链）；顺带修 EnsureGoRefs 守卫（槽位重建后能重新解析面板）。文案源 = 玩法文本.docx（初稿，待文案确认）。验收：无玩法时 3 按钮"未加载"禁用 + 3 个 None 占位；玩法激活后按钮变玩法名可点、槽位换对应面板；点按钮弹详情（名称+介绍）；选中棋子 Grp_Mode 隐藏、取消恢复。
- ✅ **构筑/后续事件无法进入——事件选项高度测量 NRE（2026-08-26 已修复，待人工验收）**：根因 = EventOptionView.MeasuredLines 直接读 _content.textInfo.lineCount，但事件面板选项区在面板 inactive 下实例化（BuildOptions 先于 SetActive(true)），TMP Awake 延迟 → textInfo 为 null → NRE，监听者异常中断 ShowEvent，面板永远不激活（事件推进卡死，无法进入构筑面板）。修复（仅 EventOptionView.cs）：① MeasuredLines 对 textInfo==null 兜底 1 行（不再 NRE）；② 新增 OnEnable 补测真实行数（面板激活后 TMP 初始化完成，多行描述高度正确）。首发能力事件不炸因 Bootstrap 回填路径先显示面板再建选项；后续事件（含构筑）均走隐藏→显示路径故必炸。编译 0 错误。验收：Play 从编辑推进到构筑事件，选项正常显示、多行描述高度正确、事件面板正常出现。
- ✅ **主菜单开场动画 v2（2026-08-26 已接，待人工验收）**：冷启动首次进主菜单播放（返回主菜单不播）——主标题 → 副标题 → 背景缓亮（纯黑→#808080）+ 按钮组（CanvasGroup 控透明，按钮恒可用态）三段依次渐入、相邻段 0.5s 重叠，总约 7.9s（无闪动；DOTween 缓动——标题/副标题 OutCubic、背景 InOutQuad（4.5s）、按钮 OutQuad，非逐帧 lerp 更丝滑）；动画期全屏透明阻射线层，任意键/鼠标左键/触摸点击 = 跳过直落终态（BGM 满音量）；BGM 黑屏阶段即 Play()（音量 0——播放初始化卡顿被黑屏盖住）、1.9s 后（副标题出现时）开始 3s 淡入（跳过=直落满音量）。**启动即黑屏**：Bootstrap Awake 首帧创建全屏黑罩（ScreenSpaceOverlay）盖住所有面板异步加载过程，主菜单开场动画接管黑屏后销毁。实现：MainMenuPanel 开场协程 + AudioManager.PlayBGM(fadeSeconds)/PreloadBGM/CompleteBGMCrossfade + Bootstrap 黑罩。验收：冷启动全程无加载闪现、动画节奏/重叠、跳过、退出战斗回主菜单不播动画。
- ✅ **启动报错消除（2026-08-26）**：PanelBase.CreateAsync 新增 Addressables 注册探测（Addressables.ResourceLocators Locate——未注册 key 直接纯代码创建，不再触发缺 key 内部报错）+ 可选 addressOverride 参数；DeckLibraryPanel 经 Bootstrap 显式加载既有 key DeckLibrary（真实 prefab 布局）；TutorialPanel 未注册走纯代码兜底（新手教程待办，无 prefab 前不报错）。验收：冷启动控制台 0 错误。
- ✅ **继续按钮可用性置灰（2026-08-26）**：SaveManager.PeekGameStateJson（轻量读主档 GameState 段，不加载快照）+ Bootstrap.CanContinueGame（镜像 TryResumeSavedState：非终局档且有进行中状态——战斗中/编辑中断/事件中断）+ MainMenuPanel.SetContinueAvailable（无可用存档置灰 Btn_ContinueGame）。验收：无存档/终局档/主菜单退出存档时按钮置灰；有进行中事件/编辑/战斗档时可用。
- ⚠️ **Bootstrap.cs 截断修复（2026-08-26，用户批准）**：工作区 Bootstrap.cs 曾因他人 WIP 保存截断（927 行中断，UI 程序集编译失败）；经批准用 HEAD 尾部补全（保留 WIP 前 927 行全部插入）+ 补 CreateLoadingPanel（协作者新增调用、方法体随截断丢失——按同款常驻创建模式补全）。编译 0 错误 0 警告。⚠️ 协作者：若本地另有完整版 WIP，请以当前文件为准或 diff 核对，勿整文件覆盖。

- ✅ **战斗场景前后层级统一排序**：棋子按行排序、阴影最底、手牌50/列表60/Tooltip1000 分层排序已代码配置。验收：常驻信息不被遮挡、弹窗可置顶。
- ✅ **行动经济 buff 显示**：BuffDisplay 按棋子输出 action_economy/action_economy_acted（ActionEconomyBuffSync 订阅既有事件补发刷新）。验收：获能力后显示态 A，执行后态 B，新回合回态 A。
- ✅ **插入执行（免费行动）UI 形态**：提示条+自动锁定棋子+弹目标选择+允许空放（TryStartForcedExec/ShowFreeActionTip）。验收：击杀触发→提示、锁定、目标选择、空放、自动收尾。
- ✅ **回合结束手动化可选增强（ap-empty 提示）**：BattleController 消费 StateChanged("ap-empty")，结束回合按钮 Tooltip 提示（1.6s 自动消失）。
- ✅ **通用变亮通道接入**：监听 HintRequested；CardQualify 手牌变亮+能力面板金色脉冲，按 EditedCardQualifyId 回填。
- ✅ **能力事件显示修订**：描述区去掉候选清单；遗物图标改代码色块；能力栏兼容回退 displayName。
- ✅ **战斗音效补挂**：玩家判负 SfxDeathUi（BattleResultPanel）+ promote/draw/shield/free/score 复核在位（音频挂点清单 2-6 条）。
- ✅ **UI/事件面板碰撞音**：UiSfx.Play 统一碰撞音覆盖 12 文件 23 处（音频挂点清单第 7 条）。
- ✅ **开场剧情接入**：story_opening.json（22 条）+ StoryPanel 播放器（新局插播、逐字、双立绘、跳过/下一句、剧情三音效）+ 导入菜单。验收：新局必播→播完进首事件、播放/跳过手感；prefab 菜单残留已清、`Img_Character_R` 已挂 Tester_Default 立绘并注册 Addressables（2026-08-26 审计）
- ✅ **编辑界面问题①：默认模块解除锁定（2026-08-26 修正：仅移动/攻击解锁；效果槽锁定）**：InitLockedFlags 按槽内容类型锁定（`slot is EffectTemplate` → locked；移动/攻击全解锁）——默认移动/攻击模块可拖出/替换/移除/重排，默认内置效果保留原位不可动（取消效果编辑定案）；被替换模块回候选库需后端候选池差集动态化（已记入后端待办）。验收：移动/攻击槽可随意更换；效果槽不可拖出/替换。
- ✅ **事件小文本接入（2026-08-25 已接）**：新增 `Assets/_Project/UI/EventTexts.cs` 显示层覆盖表（来源 `Assets/test/docs/事件小文本.docx`）——能力「“牧场”的漏洞」/编辑「车间」/构筑「组装车间」标题+描述替换；docx 斜体段 → TMP `<i>` 富文本同步；能力事件「刷新次数：N」动态注入（N = 全部候选剩余刷新之和）；未登记事件回退定义原文本。**数据域（events.json/EventDefinition 资产）未动**（红线）；玩法选择事件未接入（用户定案）。验收：三事件新文本+斜体、能力刷新一次后 N-1、其余事件文本不变。
- ✅ **事件面板左侧进度条（2026-08-25 已接，prefab 已由李毕补全）**：`EventPanel.RefreshProgress` 按 `GameState.NodeStates` 换图——Completed = 对应尺寸 filled（大节点=战斗关 60×60 / 小节点=事件 40×40）、其余 = empty（明暗由素材表达，不再颜色叠加）；节点命名 `Img_Node_{层}_{序号}`（1 起），非当前层不动；素材 Addressables 懒加载（event_circle_{small,big}_{empty,filled}，Art 组已注册）。刷新点：面板每次显示（RefreshLayout 顺带）+ 素材就绪补刷。验收：事件推进后已完成节点变 filled、当前/未来保持 empty、进层后按新层节点刷新。
- ✅ **玩法事件文本补齐（2026-08-25 已接）**：docx 更新后新增第四节——rule_pick「终端黑墙」+ 三句描述（`<i>` 斜体），`EventTexts` 四事件文本齐（能力/玩法/编辑/构筑）。验收：玩法事件显示新文本。（2026-08-26 修复：EnterRuleMode 标题此前用资产原文 `ev.title` 未走覆盖表——已改 `EventTexts.TitleFor`，标题/描述同源。）
- ✅ **获得能力弹窗确认门控（2026-08-25 已接）**：`ItemGettingPanel.IsShowing` 静态标记（展示中=true、队列清空确认=false、销毁兜底）；`EventPanel` 三入口（ShowEvent/能力/玩法）在弹窗展示中延迟存 `_deferredEventId`，确认后 Update 按事件类型冲刷（能力/玩法走状态回填、普通走 ShowEvent）——下个事件只在确认后切换。验收：选能力→弹窗不确认→下个事件不切换；确认→切换。
- ✅ **顶栏阶段名（2026-08-25 已接）**：`Txt_CurrentProgress` 阶段按层（白模/Demo/ALPHA/BETA）+ 层内节点序号（`CurrentNodeIndex`，1 起：白模-1=第 1 个事件、白模-2=第 2 个事件；战斗节点=节点总数）——2026-08-26 用户修正语义（原按层号显示废弃）；`RefreshProgress` 每次显示刷新。验收：同层事件逐个推进时数字 +1，进层切阶段名。
- ✅ **全角＋ 归一化（2026-08-26 已接）**：字体缺全角加号字形——显示层 `＋→+` 归一化（数据域 relics.json 未动）：事件选项（EventOptionView）、遗物弹窗（ItemGettingPanel）、事件标题/描述（EventTexts）。验收：能力选项/遗物描述中加号显示为半角 `+`。
- ✅ **玩法事件·二选一面板（2026-08-25 已接）**：EventPanel 玩法分支——RuleCandidatesDrawn 驱动（面板已有场景直接重建；首发/读档走 Bootstrap 懒加载后 `ShowRuleEventFromState` 回填）；复用事件面板 + Btn_EventOption 模板，2 选项无刷新、退出按钮置灰、防重复点击锁；选择 → `Resolver.SelectRule(index)`；玩法候选抽取同步落档（OnRuleCandidatesForSave）。验收：rule 节点显示二选一、点选激活玩法并推进、退出置灰、中途存档读档回填候选。
- ✅ **事件 CG 切换（2026-08-25 已接，图+位）**：EventPanel `Img_EventArt` 按事件类型换图+换位（锚点居中，直接设 anchoredPosition/sizeDelta）——能力=event_ability@(67,0,1226,868)、玩法=event_mode@(-38,0,1228,868)、编辑=event_edit@(315,0,1376,974)、构筑=event_edit@(-433,-187,1757,1242)（**构筑复用编辑图**——4 事件 3 图，2026-08-25 用户定案）；其他回退 prefab 默认图/默认位置（含模式间切换恢复）；CG 已注册 Addressables（Art 组，地址=文件名）；新增工具「工具/事件 CG 进 Addressables」（`EventArtTools.cs`）。验收：四类事件图与位置/尺寸正确、普通事件恢复默认、CG 未加载完不闪默认图。

- ✅ **顶部进度·心电图式波次进度条（2026-08-26 已接）**：`WaveProgressBar` 自动附加 BackgroundCanvas 按 waveDefs 生成段（大/中/小/空波）；「工具/战斗美术进 Addressables」已注册 battle_progress/battle_wave_*（审计确认）；旧进度代码（RefreshTurnProgress/BuildWaveNodes 等）已移除。验收：Play 看心电图节奏与回合推进透明度。
- ✅ **通关条件确认窗 + 左上角关卡名（2026-08-26 已接，待人工验收）**：战斗开始自动弹 `ClearanceConditionsPanel`（新面板 Txt_Title/Txt_Info/Btn_Confirm，已注册 UIPanels）显示本关通关条件；文案由 `ClearanceTextBuilder` 配置驱动生成（胜利规则/升变规则/波次明细/得分目标——数值全来自 FloorConfig：victoryRule/targetScore/waveDefs.count/groups/spawnShield/waveScoreTarget/endCountdown；关名 = `FloorDisplayName`：白模/Demo/ALPHA/BETA）；战斗内 `BackgroundCanvas/Btn_ClearanceConditions` 点击重看（仅战斗中有效）；左上角 `Txt_CurrentProgress` 进战斗设为关名（BattleController.Init → `panel.SetFloorName`）。编译 0 错误。验收：进战斗弹窗定稿文案（四关各段数值正确）、按钮重看、左上角关名正确。
- ✅ **战斗美术分层填充（2026-08-26 完成）**：背景/8×8棋盘/棋台/隐形3D棋盘 + 前景边框 battle_foreground 已摆入 Main.unity（Image）+ 进度/波次素材已注册 Addressables。验收：Play 整体视觉。
- ✅ **主菜单与流程入口（剩余项 2026-08-26 清完）**：剧情 prefab 菜单残留已清、Img_Character_R 拼位完成（Tester_Default 已挂+注册）；Continue/设置/续玩此前已通。验收：主菜单→剧情→事件→战斗整体走一遍。
- ✅ **LoadingPanel 接入（2026-08-26 已接）**：Bootstrap 启动常驻创建（Key="Loading"，UIPanels 已注册）+ PanelTransition 面板切换过渡（压栈渐入/渐出，多处流程 ShowWithLoading）。验收：切换过渡正常、不闪白。

- ✅ **骰子前端接入（2026-08-26 复核已实现）**：`Btn_RollDice`/`Btn_DiceMove` 接线、点数显示/提示、4 向×点数步直线可达格高亮（界内/障碍/占用过滤）→ 点格反推方向发 `OnDiceDirectionSelected`。验收：投掷→点数→启动移动→高亮→点格直线移动。
  - 修复（2026-08-27）：**投掷不了根因** = Grp_Play 槽位重建（Addressables 实例化玩法面板）完成后只补绑麻将，未补绑骰子 → `Btn_RollDice` 无监听。修复 = `RefreshFloorMode` 末尾补 `EnsureDiceRefs + RefreshDicePanel`（幂等，重建后重解析）。
  - 补充（2026-08-24）：6 张点数图（`Assets/Art/Dice/Dice_1~6.png`）已转 Sprite 并接入 `Grp_FloorPlay_Dice.prefab`（`Img_Dice` 下 6 张子图 `Img_DiceFace_1~6`，代码按 `DiceValue` 显隐；编译 0 错误）。⚠️ 场景实例曾随并行会话重构移除——重新放回实例即可生效。
- ✅ **围棋入口+买子（2026-08-26 复核已实现）**：拖拽部署（`DeployGoRequest(cell)`）、剩余次数/下次颜色/提示实时刷新；买子按钮（能力持有+双玩法激活显示、TokenCount 不足置灰、hover 浮窗说明原因）；能力提示行（速攻/升值/假定/买子）。验收：部署/次数/买子闭环。
- ✅ **代币购买（2026-08-26 复核已实现）**：牌库面板购买模式（标题"选择要购买的牌"、仅弃牌区可点、点击=购买并关闭）；`BuyTokenRequest(discardIndex)` + `StateChanged("token")` 刷新。验收：购买扣币入手牌、余额不足后端拒绝。
- ✅ **五行（属性）玩法描边（2026-08-26 复核已实现）**：`ElementColors` 五色表；手牌外描边（element!=None 加 Outline）；棋子静态描边（开局/部署同步，与升变预告共存取五行色）。验收：属性玩法激活后手牌/场上描边五色、升变后保持。

- ✅ **牌库面板（浏览入口+Mask+C1 已接，2026-08-26 审计确认）**：Bootstrap `OnDeckClicked → PushOverlay("DeckLibrary")`；`Grp_Pile_Draw/Discard` 代码补 `RectMask2D` 防卡溢出；抽牌区全牌正面（含麻将点数 `CreateMahjongCard`）+每次打开随机打乱+顶部"抽牌堆（N）/弃牌堆（N）"角标。剩余：① 滚动未接（需李毕调 prefab 布局；Mask 裁剪已防溢出）；② 弃牌区麻将死亡卡不显示（并入「麻将牌表现拓展」）。验收：打开牌库看分区/麻将正面/数量角标/不溢出。
  - 后端数据源（牌库/代币购买共用，只读）：弃牌堆=墓地=`GameState.Graveyard`；`PromotedReplacedPile`/`MahjongUsedPile`/`MahjongDeadPile`/`DiscardZone.PieceDeaths+MahjongDeaths`；变化事件 `PieceDied`/`PiecePromoted`/`StateChanged("mahjong-wall")`。
- ✅ **新手教程（框架+内容+头像+面板已接，2026-08-26 审计确认）**：TutorialManager/Panel/Mask/Content 齐备，最终版 docx 集成（含 `floor_rule_intro` 下一层玩法选择教程），4 张头像（Avatar_Xeon_常态/惊讶/坏笑 + Avatar_3号_默认）已注册，TutorialPanel 已注册 UIPanels，EventOptionView 判空与面板加载地址修复在位。剩余：高亮目标名待李毕确认（不阻塞）、后端持久化契约待后端（会话内去重兜底）。验收：事件/遗物/编辑/构筑/战斗/玩法选择各段按 docx 触发、翻页/上一步/长按跳过/遮罩挖孔。
- ✅ **属性描边·实时刷新（2026-08-26 已接）**：`OnBuffsChanged(pieceId) → ApplyElementOutline`，能力「提纯」「变换」外力改写属性时描边即时刷新（信息浮窗原有刷新保留）。验收：提纯/变换后描边立即变新属性色。
- ✅ **震击墙渲染（2026-08-26 已接）**：`RebuildShockWalls` 按 `ShockWalls` 生成深灰半透明 1×1 方块（排序 300 棋子下），挂点 `StateChanged("shock-walls")` + 战斗同步 `SyncExistingPieces`；溅射已走 `DamageDealt` 逐目标飘字。⚠️ 占位视觉待李毕换正式墙素材。验收：开战 2 墙出现、续战一致、溅射飘字。

- ✅ **图标资源接入（2026-08-26，待人工验收）**：新增 Editor 工具「工具/图标资产进 Addressables」（扫描 ProgramBlock + Characters/Special，Sprite 导入 + 地址=文件名；已跑菜单注册 19 个成功）；新增 IconLibrary 启动预载（Bootstrap ②a-2）；程序块图标经 PiecePresentationMapper.ProgramIconKey 映射——移动 4 族（step/area/jump/combo）+ 攻击 5 方式（melee/melee_aoe/direct/arcing/spell），覆盖手牌卡/构筑卡/编辑面板程序库卡+信息区槽+拖拽幽灵（效果模块不画——定案）；围棋立绘 GoPiece.png 接入 PieceViewFactory.PortraitFor；牌库面板麻将卡显示 mahjong_1~9 点数图（无图回退原文本）。验收：Play 看手牌/构筑/编辑面板程序图标、围棋立绘、牌库抽牌区麻将点数图。

- ✅ **棋子编辑事件·第 2 关起左右空白修复（2026-08-26，待人工验收）**：`SetEditableDefId` inactive 只记标记不启动刷新链（刷新统一由 OnShow 全量重建）+ `OpenPieceEditor` 复用分支先激活面板再设数据（代码注释在位）。**⚠️ 补充真根因（2026-08-26 排查确认）**：三项修复落地后实测仍空——最终定位 = **Tpl_*.asset 重导（817c4af/7b0f5cf）后 `Bootstrap.prefab._templateConfigs` 24 条引用全部反序列化为 null（guid/fileID/脚本全对，AssetDatabase 引用表未刷新）→ TemplateLibrary 注册 0 → 程序库空白（连第 1 关也空）**。修复 = 编辑器强制重导 `Bootstrap.prefab`（ImportAsset ForceUpdate）→ 引用恢复 nonNull=24，prefab 文件内容无需改动。预防：资产重导后遇配置引用全 null，重导 Bootstrap.prefab 或重跑「工具/收集全部配置到 Bootstrap」。验收：新局编辑面板左右列表正常显示。
- ✅ **新手教程遮挡/模糊修复（2026-08-26，待人工验收）**：`TutorialPanel` 自身 Canvas `overrideSorting` 置顶（sortingOrder = TutorialMask.SortingOrder + 1000）——教程文字/头像不再被遮罩压暗层盖住变模糊。验收：能力/编辑事件时教程清晰可读、遮罩挖孔正常。

- ✅ **手牌麻将卡图标修复（2026-08-27 复核已修，待人工验收）**：`MahjongCardData` 麻将卡图标位改 `IconLibrary.TryGet("mahjong_type_" + point)`（IconLibrary 已注册 mahjong_type_1~9）——不再走棋子立绘兜底/士兵图。验收：手牌麻将卡显示点数图。

- ✅ **麻将牌表现拓展（2026-08-27 已全部补齐，待人工验收）**：手牌区麻将卡显示+点击摸切/拖拽打墙（BattleController）；牌库弃牌区麻将死亡卡（`PieceDeaths + MahjongDeaths`，顺序=DiscardView）；代币可买麻将（购买模式含麻将卡，索引对齐 DiscardView，后端 BuyFromDiscard 支持）；模板复用现有 Piece_Handcard（李毕定案）。验收：手牌/牌库弃牌区/代币购买三处麻将卡全链路正常。

## 本轮已完成（一句话总结）

- ✅ **知会（2026-08-24，后端改动共享 Bootstrap——无需前端配合）**：读档保留战斗面板复用，默认行为不变。
- ✅ **能力事件三选一面板**：EventPanel 能力分支+首发/读档回填；刷新并入长按候选。
- ✅ **编辑面板重构断链 NRE**：嵌套 Piece_Handcard 判空守卫，Play 全程 0 错误。
- ✅ **牌组构筑信息区 hover 不刷新**：FindDeep 定位，立绘随 hover 刷新。
- ✅ **所有界面退出按钮 → 确认弹窗**：统一 ConfirmPanel，确认保存、取消无改动。
- ✅ **存档系统对接后端**：事件/候选/检查点/退出自动落档，四快照。
- ✅ **面板进入全量刷新布局**：PanelBase.Show 统一 RefreshLayout。
- ✅ **主菜单 Continue 对接**：读档重建会话，中断恢复；续玩已接。
- ✅ **BattleController 括号修复（2026-08-24）**：阶段展示块复位，编译 0 错误。
- ✅ **主角团立绘整理（2026-08-24）**：17 张立绘导入并注册 Addressables。
- ✅ **事件面板 CG 整理（2026-08-24）**：3 张 CG 导入 Art/Event。
- ✅ 其余已完成事项（抽牌/构筑/升变预告对接、编辑候选、实时计分、战斗协议与稳定性、升变拖拽、攻击音效分发、BGM 切换、PreviewRange 修复、升变预告 buff 提示等）见 `docs/archive/前端待办-已完成事项.md`。

## 编辑界面方案沟通（问题①已定案并实施、待验收——见待验证栏；问题②③已实施并归档）

> 背景：实测编辑事件发现两个交互问题（① 默认程序模块无法替换 ② 模块可重复放置）。详见决策记录：`内部文档/决策记录/决策记录_编辑程序约束与生命周期清理_20260823.md`。

1. **问题①：默认程序模块无法替换——已定案（2026-08-24 用户：默认模块全部可编辑）+ 前端已实施（`InitLockedFlags` 仅效果槽锁定，移动/攻击全解锁），待人工验收（见待验证栏）**；被替换/移除的默认模块回候选库需后端候选池差集动态化（已记入 `docs/后端待办.md`）。原背景：`InitLockedFlags` 曾把默认程序前 N 槽绝对锁定，4 槽填满后无法更换模块。
   - **⚠️ 范围修正 + 新配合点（2026-08-24「编辑事件取消效果编辑」定案，后端已实施）**：① 编辑事件候选从 6 改 **4**（移动 2 + 攻击 2，效果不再抽取）；② 编辑面板候选池不再含任何效果模块（后端三层过滤已做，前端无需改动候选数据）；③ **前端配合：效果槽恢复锁定**——`InitLockedFlags` 按**槽内容类型**（`slot is EffectTemplate` → locked）而非前 N 槽；拖放来源/目标/移除校验对效果槽一律拒绝（默认内置效果 = 盾兵 Effect-1 / 刺客 Effect-2 / 炮手 Effect-3，保留原位不可动）。决策记录：`内部文档/决策记录/决策记录_编辑事件取消效果编辑_20260824.md`。（2026-08-26 前端已实施：`InitLockedFlags` 按槽内容类型锁定效果槽——编译通过，待人工验收）
2. **问题②：模块可重复放置 → 消耗制候选（✅ 后端已实施 2026-08-23 晚，策划定案"池子构成规则"）**——候选池 = 模板库 − **本层占用增量**；放入→候选消失；撤销/移除→候选回来；进层→消耗复原；跨层可叠加。**前端零改动**（`ProgramEdited → RefreshProgramList` 刷新链现成，自动跟随候选变化）。⚠️ 请前端确认：刷新链实测 OK 即可；若对"消失"形式有偏好可再议。详见归档。
3. **问题③：后端校验兜底（已撤销，知会）**——原"同类型同 id 拒绝落账"校验已按策划定案撤销（允许跨层叠加）；重复防护由消耗制候选保证。详见归档。

## 音频挂点方案（2026-08-23 追加——后端已就绪，挂点在前端）

> 16 个 SFX 资源已导入 `Assets/Audio/SFX/`（来自音频同学交付；`sfx_death_ui` 原名含空格已改名合规，用途已确认 = **玩家死亡提示音**）。地址常量已全部建好（`TheLaw.Core.AudioRefs`）。**你在 Unity 里跑一次菜单「工具/音频进 Addressables」即完成资源注册**（地址规则 = `SFX/文件名`，与常量一一对应）。
> 播放 API：`AudioManager.Instance.PlaySFX(address, volumeScale = 1f, pitch = 1f)`（Core 单例，随处可调；缺失只 LogWarning 不报错）。

### 地址常量总表（可直接 `AudioRefs.xxx`）

| 常量 | 地址 | 用途 | 优先级 |
|---|---|---|---|
| `SfxMove` | `SFX/move` | 移动/落地（已有挂点 ✓） | P0 |
| `SfxDeploy` | `SFX/deploy` | 部署落子（已有挂点 ✓） | P0 |
| `SfxPromote` | `SFX/promote` | 升变替换/进化（同时覆盖敌方升变闪光）✅ 已接 | P0 |
| `SfxDraw` | `SFX/draw` | 抽牌（首回合抽4/1AP抽1）＋**复用：属性复制牌获得** ✅ 已接 | P0 |
| `SfxAttackMelee` | `SFX/attack_melee` | 近战（✅ 已接） | P0 |
| `SfxAttackMeleeAoe` | `SFX/attack_melee_aoe` | 近战群攻（✅ 已接） | P0 |
| `SfxAttackDirect` | `SFX/attack_direct` | 直射（✅ 已接） | P0 |
| `SfxAttackArcing` | `SFX/attack_arcing` | 抛射（✅ 已接） | P0 |
| `SfxAttackSpell` | `SFX/attack_spell` | 法术（✅ 已接） | P0 |
| `SfxHit` | `SFX/hit` | 命中/受击（含友伤；已有挂点 ✓） | P0 |
| `SfxDeath` | `SFX/death` | 死亡（已有挂点 ✓） | P0 |
| `SfxShield` | `SFX/shield` | 护盾抵挡"铛"（与掉血区分）✅ 已接 | P1 |
| `SfxFreeAction` | `SFX/free_action` | 免费行动获得/发动"叮" ✅ 已接 | P3 |
| `SfxScore` | `SFX/score` | 回合末得分结算入账（**回合末一次性，非逐击杀；第 1 关无结算**——2-4 关敌方回合结束入账时播）✅ 已接 | P3 |
| `SfxMahjongTile` | `SFX/mahjong_tile` | **麻将 M1-M8 全部 + UI/事件 U1-U9 全部**（统一复用一套）✅ 已接（UI 碰撞音，2026-08-24） | P1 |

### 待补挂点清单（按触发场景）

**战斗内**（BattleController 表现层）：
1. **攻击按方式分发 ✅ 已闭环（2026-08-23）**：`DamageInfo.AttackMode` 字段后端已就绪（8603793）+ 前端已在 `PlayDamage` 切换为 `PlaySFX(GetAttackSfx(info.AttackMode))`（1ee07de）——5 种攻击音分发完成；（2-6 项均已接 ✅，见下）
2. **升变**（A3）✅：`OnPiecePromoted` 闪白/替换表现处播 `SfxPromote`。
3. **抽牌**（A4）✅：`OnHandChanged` 手牌数增长统一播 `SfxDraw`（首回合抽4/1AP抽1/复制牌获得同用）。
4. **护盾**（C1）✅：`PlayDamage` 按护盾抵挡区分播 `SfxShield`/`SfxHit`。
5. **免费行动**（C7）✅：`OnExtraActionGranted` 插入提示时播 `SfxFreeAction`。
6. **得分结算**（C10）✅：`RefreshScore` 按 `scoreDeductEnabled` 门控播 `SfxScore`（仅 2-4 关；第 1 关无）。⚠️ 口径疑点（2026-08-24）：仓库暂无 floor2-4 配置、后端文档"3/4 关启用"与前端"2-4 关"表述不一——待配置落地后人工验收。

**UI / 事件关**（面板层——**全部统一 `SfxMahjongTile`**）：
7. 按钮/面板开关/确认取消/遗物获得（ItemGettingPanel）/编辑拖放替换撤销/三选一面板/构筑加减牌/Tooltip——同一套碰撞音（音量可轻）**✅ 已接（2026-08-24，待人工验收）**：新增 `Assets/_Project/UI/Widgets/UiSfx.cs`（`UiSfx.Play` 默认音量 0.4）；覆盖 12 文件 23 处——PanelBase 开关（含恢复下层幂等守卫）、事件选项/继续/长按刷新、设置/退出、构筑加减、编辑拖放落账/回槽/撤销/完成、三选一确认、ConfirmPanel 确认/取消/背景、遗物获得确认、Tooltip 新浮窗、主菜单四按钮。未覆盖面：战斗面板按钮（属 BattleController 表现层）、滑杆/下拉（防刷屏）。防重叠（2026-08-24）：`AudioManager.PlaySFX` 新增 `restartIfPlaying` 标记——同种碰撞音在播时先停旧实例再从头重播；`UiSfx.Play` 已带此标记（连点/同帧触发盖旧声不叠音）。

**麻将玩法**（全部 `SfxMahjongTile`；可选音高随点数）：打出墙体/麻将破坏/牌山填数/刻子顺子/番数+1/摸切/雀头——`pitch` 传 `0.8 + 点数×0.08` 之类即可（AudioManager 内置 ±3% 随机）**✅ 战斗侧已接（2026-08-27：打出墙体/和牌/牌山[音高随点数]，见 BattleController；摸切/刻顺/番数/雀头未单挂——统一复用 SfxMahjongTile）**

**属性玩法**：复制牌获得 = 复用 `SfxDraw` ✅（走 `HandAddCard→HandChanged→手牌数增长统一播`）

> ✅ 攻击按方式分发与 BGM 主菜单/战斗/事件/结算切换已完成（2026-08-23），详见 `docs/archive/前端待办-已完成事项.md`。

### 未交付 / 挂起（无需等，接完以上即可先上）

- `SFX/countdown`（末波倒计时 P3——资源未交付，可延后）
- `SFX/mahjong_hu`（和牌加重版——可做可不做，未交付）
- `SFX/ui_test`（设置面板音量试听 P2——未交付）
- ✅ **`SfxDeathUi` 挂点（2026-08-24 已接，待人工验收）**：2026-08-23 设计确认 = **玩家死亡提示音**（我方判负/玩家死亡时播放）——`BattleResultPanel` 判负表现处播 `SfxDeathUi`（默认音量）。
- ⏳ **开场剧情音效 ×3（2026-08-23 资源已交付，挂点待前端）**：`SfxStoryWallBreak`（墙壁碎裂）/ `SfxStoryScrape`（摩擦沙沙）/ `SfxStoryStatic`（雪花屏）——**开场剧情演出音效**；挂点建议：剧情面板/开场演出动画处按剧情节奏播放（`AudioRefs` 常量与资源已就绪；剧情面板接入见高优先级"主菜单与流程入口"条目）**✅ 已接（2026-08-24）**：`StoryPanel.cs` 播放器按 `cue.sfx` 播 `SfxStoryWallBreak/SfxStoryScrape/SfxStoryStatic`（关键词+常量名+地址三兼容）。

## 不在此文件跟踪

- 后端规则：见 `docs/后端待办.md`。
- 跨端 / 策划决策：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。
