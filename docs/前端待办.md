# 前端待办

> 当前唯一前端任务源。跨端 / 策划决策见 `docs/待确认.md`。
> 已完成事项归档：`docs/archive/前端待办-已完成事项.md`（本文件只保留未完成任务、前置条件与人工验收项）。

> 😩 **吐槽（2026-08-23，后端代发）**：前端待办越来越多了……感觉会好辛苦……谁把我的美好时光当海苔给吃掉了？

## 高优先级

- [ ] **主菜单与流程入口**：完成 Continue、设置、剧情入口；接入剧情面板；Continue 支持中断后的完整流程恢复。
- [ ] **战斗美术分层填充**：按 UI 平面方案拼接背景、8×8 棋盘、棋台景框、前景边框与顶部进度素材；保留隐形 3D 棋盘。
- [ ] **战斗场景前后层级统一排序**：统一 3D 棋子、3D TMP、SpriteRenderer、战斗面板、Tooltip、弹窗 Canvas 的 sorting layer/order；确保常驻信息不被棋盘或 UI 遮挡，弹窗可置顶点击。
- [ ] **牌库面板**：`Btn_Graveyard` 已改显示名"牌库"；点击后弹出牌库窗口，分区显示抽牌区、弃牌堆、墓地。待补牌库面板 prefab、按钮事件、弃牌堆数据与关闭/层级规则。
- [ ] **新手教程**：覆盖摆放、程序执行、抽牌、敌方波次与升变概念；升变说明为"玩家回合有 1 AP 时，将升变手牌拖到任意未升变己方棋子上"。

- [ ] **行动经济 buff 显示**（2026-08-22 后端已完成）：
  - **策划原文**："执行棋子的行动不消耗行动点，你场上的所有棋子一回合只能行动一次。"
  - **语义（已确认）**：玩家对该棋子一回合只能**执行一次行动**（不是"只能动一次"）；执行免费；免费行动/额外行动穿透该限制
  - 后端状态：已行动集 `GameState.ActionEconomyActed`（玩家回合开始重置）；buff 文案已备 `action_economy`（本回合可免费行动一次）/ `action_economy_acted`（已行动——不能再普通执行）
  - 前端需要：BuffDisplay 按棋子状态显示两项之一（与回合重置同步刷新）

- [ ] **插入执行（免费行动"获得即立即执行"）UI 形态（待沟通）**：
  - **策划原文**："免费攻击一次 → 必须立刻执行攻击（插入性行动）"（2026-08-19 确认——无保留语义）
  - **后端已实现（2026-08-22）**：击杀授予免费行动 → 当前一段执行收尾后**强制该棋立即执行**（free 额外行动）——当前为朴素"强制进入该棋执行选格"
  - **前端 UI 形态待定**：自动弹目标选择？强制锁定该棋子？该棋无目标时如何处理（空放语义）？——与 docs/待确认.md 免费行动执行方式同源，请前端沟通后定形态
  - 注（2026-08-23 更新）：能力"抽到编辑牌→立即部署/升变+执行"（E5）**后端已实现**——与插入执行同链：AP 抽牌抽到被编辑棋子牌 → 按牌型自动部署/升变 → 立即执行一次（**前端零新 UI**——全自动流程，表现走既有事件链）；本条目（免费行动插入执行 UI 形态沟通）仍有效

## 低优先级 / 暂缓

- [ ] **谢幕面板**：首次通关后的独立 Thanks for playing / 结局报幕；不属于普通战斗结果面板。
- [ ] **LoadingPanel 接入**：预制体已注册，尚未纳入流程。
- [ ] **表现队列收尾防御**：未来表现链复杂后，销毁控制器前等待表现队列排空；当前 `ResetState` 已兜底，无已知可达故障。

## 编辑界面方案沟通（问题①仍待前端反馈；问题②③已实施并归档）

> 背景：实测编辑事件发现两个交互问题（① 默认程序模块无法替换 ② 模块可重复放置）。详见决策记录：`内部文档/决策记录/决策记录_编辑程序约束与生命周期清理_20260823.md`。

1. **问题①：默认程序模块无法替换（仍待前端反馈）**——现状 `InitLockedFlags` 把棋子默认程序前 N 槽全标"锁定"（绝对固定，不可拖入/拖出/当目标），4 槽填满后无法更换任何模块，与"4 槽重排/替换"编辑语义冲突。**后端无法处理（交互层），请前端评估调整方向**：解除锁定？可替换但需确认？仅保护未编辑状态？（后端配合点：无——纯交互语义）
2. **问题②：模块可重复放置 → 消耗制候选（✅ 后端已实施 2026-08-23 晚，策划定案"池子构成规则"）**——候选池 = 模板库 − **本层占用增量**；放入→候选消失；撤销/移除→候选回来；进层→消耗复原；跨层可叠加。**前端零改动**（`ProgramEdited → RefreshProgramList` 刷新链现成，自动跟随候选变化）。⚠️ 请前端确认：刷新链实测 OK 即可；若对"消失"形式有偏好可再议。详见归档。
3. **问题③：后端校验兜底（已撤销，知会）**——原"同类型同 id 拒绝落账"校验已按策划定案撤销（允许跨层叠加）；重复防护由消耗制候选保证。详见归档。

## 音频挂点方案（2026-08-23 追加——后端已就绪，挂点在前端）

> 16 个 SFX 资源已导入 `Assets/Audio/SFX/`（来自音频同学交付；`sfx_death_ui` 原名含空格已改名合规，用途已确认 = **玩家死亡提示音**）。地址常量已全部建好（`TheLaw.Core.AudioRefs`）。**你在 Unity 里跑一次菜单「工具/音频进 Addressables」即完成资源注册**（地址规则 = `SFX/文件名`，与常量一一对应）。
> 播放 API：`AudioManager.Instance.PlaySFX(address, volumeScale = 1f, pitch = 1f)`（Core 单例，随处可调；缺失只 LogWarning 不报错）。

### 地址常量总表（可直接 `AudioRefs.xxx`）

| 常量 | 地址 | 用途 | 优先级 |
|---|---|---|---|
| `SfxMove` | `SFX/move` | 移动/落地（已有挂点 ✓） | P0 |
| `SfxDeploy` | `SFX/deploy` | 部署落子（已有挂点 ✓） | P0 |
| `SfxPromote` | `SFX/promote` | 升变替换/进化（同时覆盖敌方升变闪光） | P0 |
| `SfxDraw` | `SFX/draw` | 抽牌（首回合抽4/1AP抽1）＋**复用：属性复制牌获得** | P0 |
| `SfxAttackMelee` | `SFX/attack_melee` | 近战（✅ 已接） | P0 |
| `SfxAttackMeleeAoe` | `SFX/attack_melee_aoe` | 近战群攻（✅ 已接） | P0 |
| `SfxAttackDirect` | `SFX/attack_direct` | 直射（✅ 已接） | P0 |
| `SfxAttackArcing` | `SFX/attack_arcing` | 抛射（✅ 已接） | P0 |
| `SfxAttackSpell` | `SFX/attack_spell` | 法术（✅ 已接） | P0 |
| `SfxHit` | `SFX/hit` | 命中/受击（含友伤；已有挂点 ✓） | P0 |
| `SfxDeath` | `SFX/death` | 死亡（已有挂点 ✓） | P0 |
| `SfxShield` | `SFX/shield` | 护盾抵挡"铛"（与掉血区分） | P1 |
| `SfxFreeAction` | `SFX/free_action` | 免费行动获得/发动"叮" | P3 |
| `SfxScore` | `SFX/score` | 回合末得分结算入账（**回合末一次性，非逐击杀；第 1 关无结算**——2-4 关敌方回合结束入账时播） | P3 |
| `SfxMahjongTile` | `SFX/mahjong_tile` | **麻将 M1-M8 全部 + UI/事件 U1-U9 全部**（统一复用一套） | P1 |

### 待补挂点清单（按触发场景）

**战斗内**（BattleController 表现层）：
1. **攻击按方式分发 ✅ 已闭环（2026-08-23）**：`DamageInfo.AttackMode` 字段后端已就绪（8603793）+ 前端已在 `PlayDamage` 切换为 `PlaySFX(GetAttackSfx(info.AttackMode))`（1ee07de）——5 种攻击音分发完成；剩余同类挂点：升变/抽牌/护盾/免费/得分（见下 2-6 项）
2. **升变**（A3）：成功升变时的闪白/替换表现处播 `SfxPromote`
3. **抽牌**（A4）：手牌抽取/补牌时播 `SfxDraw`（"抽牌动作"处——首回合抽4、1AP抽1、复制牌获得同用）
4. **护盾**（C1）：护盾抵挡掉血时播 `SfxShield`（与命中音要能区分）
5. **免费行动**（C7）：`ExtraActionGranted`——免费行动"获得即立即执行"的插入提示时播 `SfxFreeAction`
6. **得分结算**（C10）：敌方回合结束、`Txt_TurnScore` 入账跳动时播 `SfxScore`（仅 2-4 关；第 1 关无——按 `CurrentFloorConfig.scoreDeductEnabled` 或楼层判断，口径问后端）

**UI / 事件关**（面板层——**全部统一 `SfxMahjongTile`**）：
7. 按钮/面板开关/确认取消/遗物获得（ItemGettingPanel）/编辑拖放替换撤销/三选一面板/构筑加减牌/Tooltip——同一套碰撞音（音量可轻）

**麻将玩法**（全部 `SfxMahjongTile`；可选音高随点数）：打出墙体/麻将破坏/牌山填数/刻子顺子/番数+1/摸切/雀头——`pitch` 传 `0.8 + 点数×0.08` 之类即可（AudioManager 内置 ±3% 随机）

**属性玩法**：复制牌获得 = **复用 `SfxDraw`**（无独立资源）

> ✅ 攻击按方式分发与 BGM 主菜单/战斗/事件/结算切换已完成（2026-08-23），详见 `docs/archive/前端待办-已完成事项.md`。

### 未交付 / 挂起（无需等，接完以上即可先上）

- `SFX/countdown`（末波倒计时 P3——资源未交付，可延后）
- `SFX/mahjong_hu`（和牌加重版——可做可不做，未交付）
- `SFX/ui_test`（设置面板音量试听 P2——未交付）
- ⏳ **`SfxDeathUi` 挂点（用途已确认，挂点待前端）**：2026-08-23 设计确认 = **玩家死亡提示音**（我方判负/玩家死亡时播放）——挂点建议：玩家判负（我方全灭/失败结算）表现处播放（`SfxDeathUi` 常量/资源已就绪）
- ⏳ **开场剧情音效 ×3（2026-08-23 资源已交付，挂点待前端）**：`SfxStoryWallBreak`（墙壁碎裂）/ `SfxStoryScrape`（摩擦沙沙）/ `SfxStoryStatic`（雪花屏）——**开场剧情演出音效**；挂点建议：剧情面板/开场演出动画处按剧情节奏播放（`AudioRefs` 常量与资源已就绪；剧情面板接入见高优先级"主菜单与流程入口"条目）

## 本轮已完成（2026-08-23）

- ✅ **🔴 紧迫·能力事件三选一面板 + 每项刷新一次**：已实现并通过编译（EventPanel 能力专用分支 + Bootstrap 首发唤醒/读档回填；未新增面板/prefab/按钮；刷新并入**长按候选选项**，无独立刷新按钮）。实现详情见 `docs/archive/前端待办-已完成事项.md`。
  - **前置条件（人工执行）**：面板代码已就绪，但能力事件资产（`Event_ability_pick` + `Relic_ability_*` ×5）当前仍处于**临时回退未注册**状态——请在 Unity 依次执行两个菜单恢复注册：
    1. **工具 / 导入关卡配置（JSON）**（生成/更新 `Event_ability_pick` + `Relic_ability_*` 资产）
    2. **工具 / 收集全部配置到 Bootstrap**（九类配置资产一键注册进预制体）
  - **人工验收步骤**：
    1. 跑完上述两个菜单后确认：`Event_ability_pick` 与 `Relic_ability_*` 已生成并注册；Bootstrap 已包含能力事件/能力遗物配置；能力池不再只指向旧普通遗物事件。
    2. 新局跑到能力事件节点：EventPanel 显示事件标题/描述，描述末尾含"长按选项按钮刷新事件（刷新次数）"与 3 行"能力候选 N：能力名称（长按刷新 · 剩余 M 次） + 描述"；选项区只显示 3 个候选选项（`displayName + 描述`）——**刷新不占独立按钮**（长按选项按钮 = 刷新该项，防选项过多溢出）。
    3. 长按某个候选选项约 0.6 秒：对应候选被替换、该项剩余次数 -1（描述中同步更新）；剩余 0 次后长按不再生效；短按 = 选择该项（长按后抬起不会误选）。
    4. 点任一候选：获得能力遗物（RelicObtained 弹窗）→ 事件面板关闭并推进到下一节点；快速连点不会重复落账（防重复点击锁）。
    5. 能力事件中退出按钮置灰不可用；普通事件退出/选项流程不受影响。
    6. （可选）能力事件中途存档再读档：面板加载后自动回填候选，不依赖历史广播。
- ✅ **🔴 紧急·编辑面板重构断链 NRE**：已核实修复（2026-08-23 运行时验证）。当前 `PieceEditPanel.cs` 已按嵌套 `Piece_Handcard` 适配（`ResolveNodes` 用 `FindDeep` 找 `Piece_Handcard`，槽位数组在判空前分配；`FillPieceInfo`/`CollectInfoSlotTargets` 均带判空守卫），旧报告行号对应旧版源码、已过时。Play 模式走完整流程（新局 → 事件 → 编辑候选 → 编辑面板 → `SelectPiece` → `FillPieceInfo`）**0 错误**；未改代码。验收：编辑面板打开无 NRE、选中棋子信息区正常。

- ✅ **牌组构筑信息区 hover 不刷新（2026-08-23 已修复）**：根因 = prefab 信息区已重构为嵌套 `Piece_Handcard`（同 PieceEditPanel），但 `DeckBuildPanel.ResolveNodes` 仍用旧 `Grp/Grp_PieceInfo` 路径 → 信息区引用全 null → `CardHover→ShowPieceInfo` 无数据可刷。修复：`ResolveNodes` 改 `FindDeep("Piece_Handcard")` + 子节点（Img_InfoProgram1~4 / Txt_InfoProgram1~4Desc / Img_InfoValue / Txt_InfoName / Img_InfoPortrait），并补立绘随 hover 刷新（`PieceViewFactory.TryGetPreloadedPortrait`）。Play 模式验证：悬停 刺客(值8) ↔ 狂战士(值5) 名称/价值/立绘均刷新，0 Console 错误。类型图标为静态 Image（新卡设计，与 PieceEditPanel 一致）。
  - 补充（2026-08-23）：信息卡背景原被 `ShowPieceInfo` 强制设为 alpha=0.45 半透明——已改为不透明（`a=1`，prefab 根 Image 本身即不透明），仅此一行。
  - 补充（2026-08-23）：程序槽位空槽不再 `SetActive(false)` 隐藏——改为常显位点 + 降透明度（`EmptyProgramSlotAlpha=0.2`，与 HandCardView 一致）；空槽文字清空。Play 验证：刺客（3 槽）第 4 空槽 imgActive=True / imgAlpha=0.20，有槽 alpha=1.00。
  - 其余已完成事项（抽牌/构筑/升变预告对接、编辑候选、实时计分、战斗协议与稳定性、升变拖拽、攻击音效分发、BGM 切换、PreviewRange 修复、升变预告 buff 提示等）已移入 `docs/archive/前端待办-已完成事项.md`。

## 不在此文件跟踪

- 后端规则：见 `docs/后端待办.md`。
- 跨端 / 策划决策：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。
