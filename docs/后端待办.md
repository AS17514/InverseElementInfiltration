# 后端待办

> 当前唯一后端任务源。跨端 / 策划决策见 `docs/待确认.md`。

- ✅ **计分结算时机与策划契约不符（2026-08-19 前端审计）——2026-08-20 后端已修复**：SettleScore 原挂在 `CheckVictory` 开头（每次请求结束即结算，基础分中途清零——不符策划"敌方回合结束才结算"）。**修复**：① 移除 `CheckVictory` 开头结算 ② 主结算移到 `EndEnemyTurn`（敌方回合收尾——"对手的该回合结束"；WipeOut/第 1 关不结算）③ `EndBattle` 终局兜底（玩家回合内全灭/判负/末波强制前补结算，防丢最后击杀分；幂等）。**前端影响**：`Txt_TurnScore`/`BaseScore` 现在玩家回合持续显示，`PlayerScore`/`WaveScore` 仅回合结算后跳变 ✓（此前实时显示不可信的问题已消除——前端可放心刷新）。

- **WipeOut 胜利语义——✅ 已确认（2026-08-20 用户：守完所有波次且全灭）**：第 1 关胜利 = A（现状）——`AllWavesDeployed() && IsEnemyWiped` 即最终语义，不改（玩家中途全灭后需等剩余波出完——策划明确如此）。顺带：敌方回合逐步行动中间未检查（失败判定延迟半回合）——低影响防御项，保留观察。

- **扣分规则（第 3/4 关可能需要——2026-08-20 策划口述）——✅ 后端已实现（开关按关启用）**：我方棋子被**敌方棋子**击败（限定棋子——麻将不是棋子）→ 本关总得分减被击败棋子价值。实现：`FloorConfig.scoreDeductEnabled`（floor.json 配开关，第 3/4 关配置时开启）+ `GameState.CurrentFloorConfig`（进层设置）+ `Resolver.DeductScoreForPlayerLose`（独立接口——扣分 clamp ≥0，供后续调用）+ `HandleDeath` 挂点（victim=我方棋子 且 killer=敌方棋子 且开关开）。原文/口径还原见内部《扣分规则_策划口述_20260820.md》。

## 高优先级（✅ 全部完成——2026-08-20）

- ✅ **修复新局牌区初始化与有效类型口径不一致——已完成**：`bb49036` 将新局分牌统一为 `GetEffectiveType`；后端统一牌区入口（`HandAddCard/HandRemovePiece/HandRemoveMahjong/DeckSetHand/DrawFromPile/DrawPileAdd`）+ `LogPileWrite` 阶段不变量诊断（Placement 阶段手牌只应含初始牌）——口径统一 + 分区不变量检查落地（BattleFlow/UI 对牌区只读）。
- ✅ **部署/升变重复牌误删——已完成**：部署、升变先定位实际消耗牌（优先携带属性的同 `defId` 卡），再经 `HandRemovePieceAt` 只移除该索引；`PieceInstance.element` 与实际消耗牌一致，避免 `RemoveAll(defId)` 误删重复牌。
- ✅ **补充牌区状态变更与部署后的后端诊断——已完成**：每个统一牌区入口统一发 `HandChanged` + `LogPileWrite` 诊断（阶段 + 变更原因）；前端手牌计数不一致时可对照日志定位。
- ✅ **Attack-11 模板——已澄清（悬空条目，非缺失）**：Attack-11 是**内置编号**（Attack≤11 属棋子默认程序区），无任何棋子引用（grep 棋子 JSON 无攻击 id 11）；按双池模型本就不在外部模板库（templates.json 从 12 起）。前端按需查描述故永不显示——系静态一致性检查暴露的**孤儿数据（测试遗留·内置预留）**——已在 `slot-descriptions.json` 标注"（[测试遗留·内置预留] 临时自定义数据——无模板/资产引用，悬空条目勿启用）"。**不补模板**；若未来某棋子要用 Attack-11 再按内置/外部定。

## 前端对接所需契约（后端侧——2026-08-21 更新）

- ✅ **战斗 Card 实例标识与精确消费——已完成（2026-08-21）**：`Card.instanceId`（统一入口分配 / `_nextCardId` 计数器入档 / 读档与新局初始化重分配 / `LogPileWrite` 唯一性断言）；`DeployRequest/PromoteRequest` + `DeployAction/PromoteAction` 携带 `cardInstanceId`，Resolver 按 id 精确消费（失败即拒绝——不隐式乱打；旧请求缺省回退）；`DeployInfo/PromoteInfo` 事件保留实例信息。**前端对接**：请求带上玩家点击那张牌的 `instanceId`（按新协议）。
- ✅ **麻将摸切空牌堆后的 HandChanged——已确认满足（2026-08-21）**：`HandRemoveMahjong`（摸切移除麻将的统一入口）**移除时即发 `HandChanged`**——即使抽牌堆为空、后续 DrawCard 无操作，手牌 UI 也会按 Hand 重建（该麻将已不在）。**待前端验证确认**（此条原基于旧版观察，现状已满足）。
- ⚠️ **表现回执 token 与状态变更事件——部分完成**：`PresentationFinished` 增 `battleSessionId/actionId`（`PresentationInfo`——前端从 `BattleFlow.CurrentPresentationToken` 读取原样带回；不匹配忽略）✅ 已完成；**超时降级**（`PresentationTimeoutSeconds` 可禁用 + LogError 预警 + 存档诊断节）✅ 已完成；协议见 `docs/表现回执与超时降级协议-给前端.md`。**`PieceStatsChanged` 事件未建**——当前"属性升变改变倍率"已由 `AddMultiplier → StateChanged("score")` 覆盖（部分满足）；待前端确认该事件所需的携带数据形态后再定。

## 中优先级 / 长期

- [ ] **`EventNodeSystem.OnTargetSelected`**：实现事件需要玩家指定场上棋子或格子时的手动目标选择。当前没有实际使用事件，暂作长期能力保留。

## 本轮已完成

- 回合计分结算：常规分数已从每次 `CheckVictory` 移到敌方回合结束；失败、全灭终局、强制判定保留必要兜底。前端实时计分同步完成。
- 编辑候选后端链路：事件级候选抽取、`GetEditCandidates`、`TryRestoreBuiltinModule`、show/hide 数据契约已存在；前端对接已完成。
- 抽牌堆与构筑规则后端实现已完成。
- AOE 逐目标伤害事件与空放事件已完成，前端组内并行表现已完成。
- TemplateDef 资产生成、Bootstrap 注册和模板库加载已完成；Attack-11 悬空已澄清（内置预留，不补）。
- **牌区统一入口重构（2026-08-20）**：Hand/DrawPile 唯一写收敛到 Resolver 统一区（含不变量诊断）；免费执行资格（Grant/Consume）同样收敛——BattleFlow/UI 对牌区/资格只剩只读。
- **计分统一入口（2026-08-20）**：BaseScore/倍率/PlayerScore 全部经 AddBaseScore/AddMultiplier/AddPlayerScore 等统一方法（麻将破坏 +1 补发计分事件；扣分 clamp 收敛一处）——计分状态零直写残留。

## 不在此文件跟踪

- 免费攻击立即插入、攻击槽选择、无攻击槽处理、资格叠加：见 `docs/待确认.md`。
- 即时失败、计分倍率来源、关卡计分配置、默认模块锁定、效果模块内容：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。
