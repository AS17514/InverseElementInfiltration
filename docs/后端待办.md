# 后端待办

> 当前唯一后端任务源。跨端 / 策划决策见 `docs/待确认.md`。

- ✅ **升变预告时序修复（2026-08-24 前端转达·用户定案，已实施，仅改 BattleFlow.cs）**：新语义——假设波次部署回合 s（大波）：**预告 s-2、升变 s-1、部署 s**（旧实现：部署当回合挂载 countdown=1、下一敌方回合升变）。换算：部署波 startTurn=s 在敌方回合 s（TurnCount=s-1），预告 TurnCount=s-3、升变 TurnCount=s-2。实现：`PendingPromoBatch`（旧 promotions 延迟挂载批次，瞬态不入档）+ `MountPromotions` 统一挂载助手 + autoPromote 前瞻（TurnCount==s-3 触发 AnnounceAutoPromotions，countdown=2→1）+ 部署循环按间隔（≥3 挂起到 s-2 / =2 当回合 / 末波保持）分派。存档兼容：GameState/DTO 零改动，countdown 语义不变。遗留：旧 promotions 目标棋子为本波自身（s-2 时未上场）——已按锚定下一波部署回合实施，floor1 两种方案可观测时间线相同；末波/首波 startTurn≤2 的 autoPromote 周期跳过；延迟批次未入档（启用旧机制需补存档字段）。⚠️ 未 Play 实测，待人工验证。

- **默认模块回库（候选池差集动态化）——2026-08-24 前端转达（用户定案）**：编辑默认程序模块改为全部可编辑（前端解除 `InitLockedFlags` 锁定）；被替换/移除的默认（内置）模块**回到候选库**。现状候选池 = 模板库 − 本层占用增量、内置模块按差集排除——需 `GetEditCandidates` 将"本层被移除的内置默认模块"动态纳入候选（`Resolver.ApplyProgramEdit` 维护），前端只解锁交互。**⚠️ 范围缩窄（2026-08-24「取消效果编辑」定案）**：回库范围 = **仅移动/攻击**——效果模块不可编辑（见下条），效果不进候选池/不回库。

- ✅ **编辑事件取消效果编辑（2026-08-24 策划定案，决策记录_编辑事件取消效果编辑_20260824）**：编辑事件候选从 6 改 **4**（移动 2 + 攻击 2）；**效果不进候选池**（事件级候选 / 无事件回退模板库 / 内置差集三层全过滤）；默认程序内置效果（盾兵 Effect-1/刺客 2/炮手 3）**保留原位、不可被移除/替换**（效果槽锁定——前端配合恢复，见前端待办）；效果机制（装配即生效/执行跳过/Effect-N 计值）**整体保留**仅编辑入口关闭。实施：`EventNodeSystem.DrawEditCandidates` 删效果组 + `EditorSession.GetEditCandidates` ①回退过滤 EffectTemplate ②内置差集过滤（防御）+ `TryRestoreBuiltinModule` 拒绝效果（双防）。⚠️ 未编译验证、未提交；**前端配合点**：`InitLockedFlags` 效果槽按槽内容类型恢复锁定（拖放/移除校验拒绝效果槽）——见 docs/前端待办「编辑界面方案沟通」。

- ✅ **计分结算时机与策划契约不符（2026-08-19 前端审计）——2026-08-20 后端已修复**：SettleScore 原挂在 `CheckVictory` 开头（每次请求结束即结算，基础分中途清零——不符策划"敌方回合结束才结算"）。**修复**：① 移除 `CheckVictory` 开头结算 ② 主结算移到 `EndEnemyTurn`（敌方回合收尾——"对手的该回合结束"；WipeOut/第 1 关不结算）③ `EndBattle` 终局兜底（玩家回合内全灭/判负/末波强制前补结算，防丢最后击杀分；幂等）。**前端影响**：`Txt_TurnScore`/`BaseScore` 现在玩家回合持续显示，`PlayerScore`/`WaveScore` 仅回合结算后跳变 ✓（此前实时显示不可信的问题已消除——前端可放心刷新）。

- **WipeOut 胜利语义——✅ 已确认（2026-08-20 用户：守完所有波次且全灭）**：第 1 关胜利 = A（现状）——`AllWavesDeployed() && IsEnemyWiped` 即最终语义，不改（玩家中途全灭后需等剩余波出完——策划明确如此）。顺带：敌方回合逐步行动中间未检查（失败判定延迟半回合）——低影响防御项，保留观察。

- **扣分规则（第 3/4 关可能需要——2026-08-20 策划口述）——✅ 后端已实现（开关按关启用）**：我方棋子被**敌方棋子**击败（限定棋子——麻将不是棋子）→ 本关总得分减被击败棋子价值。实现：`FloorConfig.scoreDeductEnabled`（floor.json 配开关，第 3/4 关配置时开启）+ `GameState.CurrentFloorConfig`（进层设置）+ `Resolver.DeductScoreForPlayerLose`（独立接口——扣分 clamp ≥0，供后续调用）+ `HandleDeath` 挂点（victim=我方棋子 且 killer=敌方棋子 且开关开）。原文/口径还原见内部《扣分规则_策划口述_20260820.md》。

## 前端建议契约（2026-08-26 前端搭建新手教程框架后提出——后端配合项，非阻塞）

- **TutorialSystem 激活钩子**：当前 `TryShow` 只标记入档、不广播（TODO 在 TutorialSystem.cs:26），前端无法感知。建议：`TryShow` 首次命中后发 `TutorialRequested(string tutorialId)` 事件（UI 监听）；激活调用点由后端在流程点埋：首次 EventOpened/AbilityCandidatesDrawn/RuleCandidatesDrawn、RelicObtained、EditCandidatesDrawn、StateChanged("deck")、PhaseChanged(Placement)、首次 DeployInfo。前端当前已用**事件观察 + 会话内去重**实现（零后端改动）；接契约后去重持久化（重启不重放）。前端不调用 TryShow（状态修改归后端）。

## 高优先级（✅ 全部完成——2026-08-20）

- ✅ **修复新局牌区初始化与有效类型口径不一致——已完成**：`bb49036` 将新局分牌统一为 `GetEffectiveType`；后端统一牌区入口（`HandAddCard/HandRemovePiece/HandRemoveMahjong/DeckSetHand/DrawFromPile/DrawPileAdd`）+ `LogPileWrite` 阶段不变量诊断（Placement 阶段手牌只应含初始牌）——口径统一 + 分区不变量检查落地（BattleFlow/UI 对牌区只读）。
- ✅ **部署/升变重复牌误删——已完成**：部署、升变先定位实际消耗牌（优先携带属性的同 `defId` 卡），再经 `HandRemovePieceAt` 只移除该索引；`PieceInstance.element` 与实际消耗牌一致，避免 `RemoveAll(defId)` 误删重复牌。
- ✅ **补充牌区状态变更与部署后的后端诊断——已完成**：每个统一牌区入口统一发 `HandChanged` + `LogPileWrite` 诊断（阶段 + 变更原因）；前端手牌计数不一致时可对照日志定位。
- ✅ **Attack-11 模板——已澄清（悬空条目，非缺失）**：Attack-11 是**内置编号**（Attack≤11 属棋子默认程序区），无任何棋子引用（grep 棋子 JSON 无攻击 id 11）；按双池模型本就不在外部模板库（templates.json 从 12 起）。前端按需查描述故永不显示——系静态一致性检查暴露的**孤儿数据（测试遗留·内置预留）**——已在 `slot-descriptions.json` 标注"（[测试遗留·内置预留] 临时自定义数据——无模板/资产引用，悬空条目勿启用）"。**不补模板**；若未来某棋子要用 Attack-11 再按内置/外部定。

## 前端对接所需契约（后端侧——2026-08-21 更新）

- ✅ **战斗 Card 实例标识与精确消费——已完成（2026-08-21）**：`Card.instanceId`（统一入口分配 / `_nextCardId` 计数器入档 / 读档与新局初始化重分配 / `LogPileWrite` 唯一性断言）；`DeployRequest/PromoteRequest` + `DeployAction/PromoteAction` 携带 `cardInstanceId`，Resolver 按 id 精确消费（失败即拒绝——不隐式乱打；旧请求缺省回退）；`DeployInfo/PromoteInfo` 事件保留实例信息。**前端对接**：请求带上玩家点击那张牌的 `instanceId`（按新协议）。
- ✅ **麻将摸切空牌堆后的 HandChanged——已确认满足（2026-08-21）**：`HandRemoveMahjong`（摸切移除麻将的统一入口）**移除时即发 `HandChanged`**——即使抽牌堆为空、后续 DrawCard 无操作，手牌 UI 也会按 Hand 重建（该麻将已不在）。**待前端验证确认**（此条原基于旧版观察，现状已满足）。
- ⚠️ **表现回执 token 与状态变更事件——部分完成**：`PresentationFinished` 增 `battleSessionId/actionId`（`PresentationInfo`——前端从 `BattleFlow.CurrentPresentationToken` 读取原样带回；不匹配忽略）✅ 已完成；**超时降级**（`PresentationTimeoutSeconds` 可禁用 + LogError 预警 + 存档诊断节）✅ 已完成；协议见 `docs/表现回执与超时降级协议-给前端.md`。**`PieceStatsChanged` 事件未建**——当前"属性升变改变倍率"已由 `AddMultiplier → StateChanged("score")` 覆盖（部分满足）；待前端确认该事件所需的携带数据形态后再定。
- ✅ **DamageInfo 透传本次攻击类型（前端攻击音对接——2026-08-23 后端已实现）**：当前 `AttackAction.template` 已含本次 `AttackTemplate.mode`，但 `DamageInfo` 未携带，导致前端无法在多攻击槽棋子上准确判断本次攻击。请在 `DamageInfo` 新增公开字段 `AttackMode AttackMode`，并在 Resolver 的单体、AOE 命中、AOE 空放三处 `new DamageInfo` 时从当前模板填入：单体 `action.template.mode`；AOE 命中/空放 `template.mode`。**前端已按此接口完成调用**：`BattleController.PlayDamage` 在同组首次攻击时读取 `info.AttackMode`，映射 `Melee / MeleeAOE / DirectFire / Arcing / Spell` 到 `AudioRefs` 的五个攻击音；未知枚举回退近战音。暂不需要模板 id 或槽位索引。**⏳ 待前端切换**：字段已就绪——`BattleController` 的攻击音行（当前近战保底）改为 `GetAttackSfx(info.AttackMode)` 即完成分发。

## 中优先级 / 长期

- [ ] **`EventNodeSystem.OnTargetSelected`**：实现事件需要玩家指定场上棋子或格子时的手动目标选择。当前没有实际使用事件，暂作长期能力保留。
- [ ] **战斗中续玩（✅ 2026-08-24 临时方案已实施——未编译验证；后续待策划确认定稿）**：**不再恢复中断状态**——用户定案"战斗直接回战斗刚开始"（SL 重打语义）：`SaveManager.SaveBattleStart/LoadBattleStart`（**SL 槽**——GameState+RNG 两快照独立 `save_battle.json`；`BattleFlow.StartBattle` 在**波次随机前**保存 → 重开与首次完全一致[含首波随机阵容]）；`TowerFlow.StartBattleAtCurrentFloor`（续玩/重开当前关）；Bootstrap Continue 战斗分支 = `LoadBattleStart` + `StartBattleAtCurrentFloor`（控制器由 PhaseChanged(Placement) 自动创建；构筑/遗物/塔进度保留——ResetForBattle 只清战斗态）。原 `ResumeBattle`（恢复中断状态）方案**搁置**。失败/死亡档不可读（失败清档已符合）；GameOver 档防御=清档回主菜单。⚠️ 判定注意：`CurrentEventId` 战斗后残留——前端已用 `PiecesById.Count>0` 判战斗（现状保持）。待策划确认点见 `决策记录_战斗中续玩_20260824.md` 三节（回开始 vs 续状态取舍/SL 刷随机/GameOver 防御）。
- [ ] **⚠️ 提醒：`_buffDescriptions` 空引用复发现象（2026-08-23）**：`Bootstrap.prefab` 的 `_buffDescriptions` 字段曾为空引用（fileID: 0——buffs-descriptions.json 从未被拖拽）→ `BuffDescTable.Load(null)` → buff 名称回退机器码/内置兜底（"行动经济""升变预告"等无兜底项只显示 key）。**修复途径**：「工具/收集全部配置到 Bootstrap」已含描述表收集（`CollectTextRefsToBootstrap` 按文件名精确匹配填入 `_slotDescriptions/_buffDescriptions/_slotValues`）——**每次跑收集工具会自动修复**。⚠️ 但**回退/恢复 prefab（git restore Bootstrap.prefab）会再次丢失此修复**（2026-08-23 资产回退即发生）——回退后记得重跑一次收集工具，并可用 prefab 检查 `_buffDescriptions: {fileID: 0}` 判别。
- ✅ **存档排查增强（2026-08-23 已实施）**：① **第一梯队（常开）**：`ReplayLog` 行动补全上下文字段（`side/defId/turn`——离线推演直读；旧档缺省兼容）+ **死亡记录**（`DeathAction` 进回放——补"死亡黑盒"缺口，killer=-1=非攻击击杀/未知来源）；② **第二梯队（诊断开关 `TheLaw.Core.Diagnostics.VerboseEnabled`——**默认开启**开发期留档取证；⚠️ **发布前记得置 false**（届时存档回零膨胀）**）**：随机用途标注（`RandomManager` 按调用序记录 `[CallerMemberName]` 来源）/ 承伤结果（护盾吸收、实际伤害、剩余）/ 攻击判定（相克/相生/普通/空放）/ AOE 命中 / AI 决策过程（移动预测/攻击评估/预算）。**使用方式**：默认已开——正常游玩即有完整诊断入档（存档含 `DiagnosticLog`/随机缓冲）；排查"伤害不对/AI 行为异常/无故死亡"类 bug 直接读档即可。（2026-08-23 收口：唯一写入口 `GameState.LogDiagnostic`（内部判开关+环形 5000 上限防膨胀）；`Diagnostics.VerboseEnabled` 属性化——变更联动单挂点。）

## 本轮已完成

- ✅ **能力池 P3：震击/吃子（2026-08-24 实施，决策记录_能力池扩展_20260824；**"编辑："前缀仅"编辑：极速"**——震击/吃子为无前缀独立能力名，tags basic）**：
  - **震击**（basic，AP+1）：`StartBattle` 时非部署区（y2-5）随机 2 格生成不可破坏墙（`GameState.ShockWalls`——战斗级入档；并入 `IsBlocked` 阻挡移动/直射；`SpawnShockWalls` RandomManager 可复现）；攻击命中震击墙（不破坏）→ 周围 8 格**所有棋子**（敌我双方）受**固定 1** 伤害（`ResolveShockWallSplash` 逐目标 DamageDealt——AOE 协议；溅射口径定案：固定 1/敌我双方）
  - **吃子**（basic，AP+1）：玩家侧执行**跳过全部攻击槽**（`AdvanceSlot` → `SkipAction(NoAttack)`——纯攻击槽程序=纯跳过，定案）；移动落点放宽（`BoardRules.CanLandOnCell`：玩家侧可踩**敌方棋子格**——路径规则不变）；踩格 = 先 `HandleDeath`（无视承伤含护盾、killer=移动棋子，计分/牌去向正常）再移动落账（顺序：死亡→移动——防按位置移除冲突）；敌方 AI 不消费
  - 配置 relics.json 23 条（+震击/吃子 tags basic）。⚠️ 未编译验证；**需用户重跑导入+收集生成 2 个新 Relic_* 资产**；前端震击墙表现见 docs/前端待办。

- ✅ **强劲（2026-08-24 实施，直射专属射程修正）**：我方棋子**直射距离 +1**（仅 `DirectFire`——与通用 `AttackRange` 修正（鹰眼符石）并存可叠加）；`RelicEffectType.DirectFireRange` + `GameState.DirectFireRangeBonus`（聚合查询——DeployRowBonus 同款）+ `BoardRules.GetAttackableCells` 两路径（方向集/rangeSteps）condition 并入（mode==DirectFire && side==Player）；玩家侧限定（敌方不消费）；截断规则与预览（PreviewRange 共用候选计算）不受影响。配置 `ability_strong`（**强劲**无前缀）tags basic [APBonus+1, DirectFireRange+1]。⚠️ 未编译验证；需用户重跑导入+收集生成资产。

- ✅ **买子（2026-08-24 实施——策划定案："买了的话是获得一次下棋的次数"）**：花固定 2 代币（effects `TokenBuyGo` value=2，费用可配置）→ 获得**一次围棋部署次数**——**当回合有效**（`GameState.GoExtraDeploys` 回合开始清、战斗边界清、入档）；容量 = 免费限次（速攻→2）+ 买子次数（`GoDeployCapacity` 单一来源——BattleFlow 校验/Resolver 防御共用）；`BuyGoRequest` 入口（不耗 AP——**豁免集已登记**，与围棋部署/代币购买同款）；**统一扣币点 `Resolver.DeductToken`**（购买/买子共用：扣减 + 节流[消耗→倍率+1] + 老虎机[花费数==骰子点数→抽1弃1] 全联动）；`BuyGoDeploy` 落账（费用/余额校验内部）。配置 `ability_go_buy`「买子」tags [go, token]——relics.json 25 条（**策划清单 23 条全部落地**）。⚠️ 未编译验证；需用户重跑导入+收集生成资产；**前端次数显示契约**（围棋入口：剩余次数=limit+extra−count、买子按钮、`StateChanged("go")` 刷新）见 docs/前端待办「新玩法前端接入——围棋」。

- ✅ **方向语义修正（2026-08-24——points/jumpOffsets 改按 facing 旋转；用户实测炮手方向错误驱动）**：根因 = points/jumpOffsets 是**绝对棋盘坐标**不随 facing 旋转，且历史配置把"前方"写成了 dy 负（朝玩家区）→ 玩家炮手（facing Up）朝下打自己后方 ✗（敌方碰巧正确掩盖问题）。**修复（方案 A）**：① `BoardRules`：points（GetAttackableCells 抛射/法术分支）与 jumpOffsets（GetLegalMoves 跳跃分支）解析时按 facing 旋转（同 directions 范式——敌方 facing Down 自动 180° 朝向我方，对称自洽）② 配置改写为"棋子朝 Up"基准（**前方 = +dy**）：炮手 Arcing-10 5 点 dy 反号；模板库 Attack-14/16/17/18/19 points、Move-15/16 jumpOffsets 全部 dy 反号；Move-18 补 (0,-2) 成 8 向对称（防旋转后不等价）③ 术士/神射手点位全向对称——旋转不变，**不改**；directions 体系（相对 facing）本就正确，零影响。⚠️ 未编译验证；**需用户重跑「导入关卡配置（JSON）」重生成炮手棋子资产与 Tpl_* 模板资产**（模板库 24 条）。

- ✅ **能力池 P2：复合 5 条（2026-08-24 实施，决策记录_能力池扩展_20260824；**买子挂起**——围棋+代币，待策划定案语义后补）**：
  - **骨骰骨牌**（麻将+骰子）：投掷 → 点数填牌山（`PushMahjongScore` 改返回"本次是否构成刻/顺"）；本次点数参与组成 → **当回合 PlayerAP+1**（策划定案：加**当前点数**非上限、**不钳制**——回合末自然清零）
  - **麻将筹码**（麻将+代币）：部署的麻将墙被破坏 → +1 代币（挂 `BreakMahjongWall`）；代币购买弃牌区麻将牌 = 现状天然支持（`BuyFromDiscard` 的 DiscardView 含麻将死亡、价值=点数 ✓）
  - **变换**（属性+骰子，AP+1）：投掷 → 场上**双方**棋子 + 手牌棋子牌**重刷随机属性**（`RerollAllElements`——口述"所有棋子"无我方限定，按字面双边）
  - **属性骰子**（属性+骰子）：骰子 1-金/2-木/3-水/4-火/5-土/**6=任意**（部署任何属性棋子触发）；部署"骰子对应属性"棋子 → 倍率+1（挂 `ResolveDeploy`，与骰子本体"部署价值==点数"独立并存）
  - **老虎机**（骰子+代币）：投掷 → +骰子点数代币；**花费代币数 == 当前骰子点数**（精确匹配定案）→ 抽一张 + 手牌随机弃一张到弃牌区（`DropRandomCardToDiscard`——棋子进墓地+弃牌区、麻将进弃牌区，宝牌判定并入）
  - 配置 relics.json 21 条（基础 5 + P1 9 + 玩法事件能力 + P2 5；tags 双词条 ↔ D1"全部匹配"语义）。⚠️ 未编译验证；**需用户重跑「导入关卡配置」+「收集全部配置到 Bootstrap」生成 5 个新 Relic_* 资产并注册**；「买子」待语义定案（花钱突破限次 A / 受限次 B / 买棋牌 C——默认建议 A）。

- ✅ **能力池 P1：单玩法 9 条（2026-08-24 实施，决策记录_能力池扩展_20260824；配置 relics.json 16 条 = 基础 5 + 玩法事件所需 + P1 9）**：
  - **麻将**：**改良**（AP+1；己方部署/升变 → 升变后棋子价值填入牌山 `PushMahjongScore`；和牌时基础得分 + 雀头价值[多个按最高 `MaxHuHeadValue`]）、**宝牌**（获得后选 1-9 数字 → `GameState.BaopaiNumber` 入档[整局]；**价值 == 宝牌数字**的牌[棋子=推导价值/麻将=点数]进弃牌区[玩家棋死/升变原牌/摸切/墙破坏四入口] → 基础得分+其价值；组成刻/顺含宝牌数字 → 番数额外+1）
  - **属性**：**提纯**（每回合开始手牌棋子牌属性 → 其**相生属性**[金→水/木→火/水→木/火→土/土→金——`ElementRules.GeneratingOf`]；部署/升变时全场己方棋子属性 → 操作棋子属性，每变化一个 → 基础得分+1）
  - **围棋**：**速攻**（AP+1；每回合围棋部署限次 1→2——`GameState.GoDeployLimit` 单一来源，BattleFlow/Resolver 共用）、**升值**（每次部署围棋 → `GoValueBonus`+1 全场叠加，**战斗级复原**；围棋击杀价值 = GoValueBonus）、**假定**（AP+1；围棋可用**手牌升变牌**升变 → 该牌棋子[IsGo 清除、真实棋子可行动；围棋无牌概念——不写弃牌区/替换池]）
  - **骰子**：**出千**（AP+1；投掷改为自选：发 `StateChanged("dice-rig-select")` → 前端 1-6 面板 → `BattleFlow.OnDiceNumberSelected(n)` → `RollDiceChosen`；瞬态不跨回合）
  - **代币**：**开源**（AP+1；击败对方棋子 → +1 代币）、**节流**（AP+1；消耗代币 → 倍率+1；购买**非初始棋子** → 返还 1 代币）
  - **前端契约**：宝牌 1-9 数字面板（`Resolver.SetBaopaiNumber(n)`）/ 出千 1-6 自选面板（`BattleFlow.OnDiceNumberSelected(n)`）——见 docs/前端待办「高优先级」。⚠️ 未编译验证；**需用户重跑「导入关卡配置」+「收集全部配置到 Bootstrap」生成 9 个新 Relic_* 资产并注册**；口径默认走策划原文字面（升变填升变后价值/相生=本体生出的元素）。

- ✅ **新玩法流派代码落地（2026-08-24，决策记录_新玩法流派_骰子围棋代币_20260824）**：**弃牌区重构**——新增 `DiscardZone` 大容器（棋子死亡 Card 化 + 麻将死亡[摸切立即/墙体破坏后]，与原 Graveyard 双写 A）；**升变瞬间原牌立刻进弃牌区**（仅玩家侧——敌方升变不写；死亡时只记升变牌当前形态——策划新语义）；**骰子**（6 面投掷=执行类行动 1 AP→点数+基础分；部署价值==点数棋子倍率+1；点数直线移动=全场 buff 下次执行重定向[选方向、正好走到终点]、不跨回合、其他行动不取消）；**围棋**（常驻"棋子牌"独立于手牌[判负不受影响]、部署不消耗、每回合 1 次、蓝红 side 切换、1 次伤害退场、不可行动/升变；**落子瞬间一次性围杀**[被围棋子四邻含本次落子格、边界算墙、包围者限围棋棋子]→提子+倍率+1、之后区域正常生存；红围棋计入 `IsEnemyWiped`；AI 挡路检测；围棋死亡不进弃牌区）；**代币**（初始 0 每回合+1 不跨战斗；购买=读 DiscardZone 选牌→耗价值数[棋子推导价值/麻将点数]→复制入手牌+基础分+消耗数，每回合不限次；初始棋子复制免费部署）；**复合词条**（能力池过滤改"全部匹配"+"basic 永远匹配"）；**波次部署替代格**（位置被占→敌方部署区空格→外一排→以此类推，不再跳过）。⚠️ 未编译验证；前端契约见 docs/前端待办「新玩法前端接入」；E1 玩法选择机制**已定案并实施**（见下条）。

- ✅ **玩法选择机制（二选一）（2026-08-24 策划定案，决策记录_玩法选择机制_二选一_20260824）**：玩法事件（改变规则事件形态）——从未激活玩法（麻将/属性/骰子/围棋/代币 5 个）随机抽 2 → 玩家二选一、不可刷新；**已选排除**（候选池 = 全部玩法 − ActiveStyles，推导排除）；**落选保留**（后续仍可能再出现）；第 2/3/4 关各 1 次（最多激活 3 个）。实施：`StyleRegistry`（玩法 id 单一来源——收编 BattleFlow/Resolver 全部字面量；`Mahjong.StyleId` 指向）+ `Resolver.DrawRuleCandidates`（池 = `StyleRegistry.All` − ActiveStyles，无放回抽 2，种子可复现；池 <2 防御）/ `SelectRule`（激活→清候选→事件完成）+ `EventDefinition.isRulePick` + `GameState.RuleCandidates`（入档——中断续玩一致；ResetForNewRun 清）+ `GameEvent.RuleCandidatesDrawn` + EventNodeSystem isRulePick 分支 + events.json `rule_pool`/`rule_pick`（临时文案待正式）。**前端契约**：二选一面板（监听 `RuleCandidatesDrawn`；复用能力事件三选一面板形态、无刷新按钮；数据 = `GameState.RuleCandidates`——见 docs/前端待办「高优先级」）。⚠️ 未编译验证；需用户重跑「导入关卡配置（JSON）」+「收集全部配置到 Bootstrap」生成 rule 事件资产（**第 2-4 关落地前不触发——安全**）；第 2-4 关 floor 配置（eventSequence 加 "rule" 节点，须在 ability 之前）待策划输入。

**⚠️ rule 节点缺失/断链的 Bug 面分析（2026-08-24，用户质询"rule 节点缺失会不会导致 bug"）**：
- **场景 A（无 bug）**：第 2-4 关 `eventSequence` 未加 rule 节点 → 玩法事件不触发，其余正常（仅玩法体系不可用）
- **场景 B（卡死）**：有 rule 节点但 `eventPoolIds` 对应槽位缺失/池名拼错/events.json 无 `rule_pool` → `OpenEvent` 池 null → `Assert.Fail` 断言 + return；且 `AdvanceNode` 已先行消耗节点（NodeStates Completed、index++）→ **节点永久卡死**（无 UI、仅断言日志、无重试路径）
- **场景 C（卡死）**：rule_pool 存在但 entries 空 → 同 B
- **场景 D（卡死·已修）**：事件资产 `isRulePick=0`（导入器漏配——已由提交 12c50d7 修复：ConfigImporter 补解析 + 资产手动置 1）→ 走普通选项流、options 空 → 事件无法完成
- **场景 E（卡死·已预防）**：前端二选一面板未接 + rule 触发（能力事件 8/23 同款教训——面板就绪前第 2-4 关不得配 rule 节点）
- **第 2-4 关配置五查（防 B/C/E）**：① eventSequence 加 "rule"（须在 ability 前）② eventPoolIds 对应槽位 = "rule_pool"（与 events.json 池名**完全一致**）③ rule_pool entries 非空 ④ 前端二选一面板已就绪 ⑤ 导入后读 `Event_rule_pick.asset` 确认 `isRulePick: 1`

- ✅ **牌去向记录体系（2026-08-24 已实施，决策记录_牌去向记录_麻将池与升变替换池_20260824）**：弃牌区 = 墓地（同一概念——出处方案描述 §4）；玩家牌死亡进墓地（记死亡时 defId=升变后形态——升变牌死亡进墓地即此，现状符合）；**升变替换池** `PromotedReplacedPile`（场上原牌被升变替换，仅玩家侧）；**麻将使用池** `MahjongUsedPile`（摸切/打出墙体——打出=使用）与**麻将死亡池** `MahjongDeadPile`（墙体破坏——从使用池**转移**，与棋子牌墓地独立）；记录**实际那张牌**（含 instanceId——每牌有 ID），墙体 `ObstacleData` 带 instanceId（破坏精确转移）；麻将请求契约零改动（后端取牌即可）；三池整局累计（ResetForNewRun 清）、入档（旧档缺省空兼容）。⚠️ 未编译验证。

- ✅ **回合结束手动化 + AP 豁免集（2026-08-23 已实施，决策记录_回合结束手动化与AP豁免集）**：AP 耗尽**不再自动结束回合**（玩家手动"结束回合"——现状按钮 PlayerTurn 恒可用，前端零改动）；AP=0 拦截全部普通玩家行动（`ProcessRequest` 统一成本预检），豁免"免费行动（request.free）"与"E5 资格牌打出"两类（`IsExemptFromApCost` 集中豁免集——**未来新免费类行动在此登记**）；`DeductActionPoint` 扣后负值钳制 + 预警兜底（防绕过路径）；前端可选消费 `ap-empty` 提示。

- ✅ **跨局残留三修（2026-08-23 用户实测驱动）**：① `ResetForNewRun` 补 `PlayerAPMax=1`/`EnemyAPMax=3`/`_nextPieceId=1`——AP 上限此前跨局泄漏（启动自动读档恢复旧档值 + ResetForNewRun 漏复位 → 每局 +1，实测第 5 局开局 5）；棋子 Id 决策 1 修正为**局内单调**（状态清理已完备，复用安全；Continue 读档恢复不受影响）② `Bootstrap` 移除**启动自动 LoadAll**（根治"新游戏继承旧档"——设置已独立 settings.json 加载，游戏状态仅"继续"按钮时 LoadAll；顺带修复每局随机种子相同问题）③ 决策记录/记忆同步。验证：删旧档→开新局→拿能力 AP=2；多次 Play 种子各不相同。

- ✅ **E5「编辑：极速」高亮资格式已实施（2026-08-23 配置入池；2026-08-24 名称定稿：抽牌即战→编辑：极速）**：抽到被编辑棋牌 → 资格授予（`GameState.EditedCardQualifyId`=牌实例 id，入档）+ 发 `HintRequested(CardQualify)`；玩家打出该牌=免费+立即执行（插入链）；其他行动/回合结束=资格作废（提示取消）；不跨回合。事件/状态契约见 `docs/前端待办.md`「通用变亮通道接入」。⚠️ 待前端按契约接高亮渲染（手牌+能力面板亮）；配置已改（relics.json displayName="编辑：极速"——重跑导入即生效）。

- 回合计分结算：常规分数已从每次 `CheckVictory` 移到敌方回合结束；失败、全灭终局、强制判定保留必要兜底。前端实时计分同步完成。
- 编辑候选后端链路：事件级候选抽取、`GetEditCandidates`、`TryRestoreBuiltinModule`、show/hide 数据契约已存在；前端对接已完成。
- 抽牌堆与构筑规则后端实现已完成。
- AOE 逐目标伤害事件与空放事件已完成，前端组内并行表现已完成。
- TemplateDef 资产生成、Bootstrap 注册和模板库加载已完成；Attack-11 悬空已澄清（内置预留，不补）。
- **牌区统一入口重构（2026-08-20）**：Hand/DrawPile 唯一写收敛到 Resolver 统一区（含不变量诊断）；免费执行资格（Grant/Consume）同样收敛——BattleFlow/UI 对牌区/资格只剩只读。
- **计分统一入口（2026-08-20）**：BaseScore/倍率/PlayerScore 全部经 AddBaseScore/AddMultiplier/AddPlayerScore 等统一方法（麻将破坏 +1 补发计分事件；扣分 clamp 收敛一处）——计分状态零直写残留。
- **配置收集工具泛化（2026-08-23）**：新增菜单「工具/收集全部配置到 Bootstrap」——九类配置资产（棋子/能力/层/地图/AI/事件池/事件/遗物/模板）一键扫描填入 Bootstrap 预制体对应列表字段，替代新增资产后的手工拖拽；原「工具/收集模板资产到 Bootstrap」保留（委托通用实现）。**使用流程**：改 JSON → 工具/导入关卡配置（JSON）增量更新资产 → 工具/收集全部配置到 Bootstrap（注册进预制体）→ 运行验证。场景实例无配置列表 override——改预制体资产即生效。

## 能力事件（2026-08-22——✅ 后端已实现核心，前端待接）

- **机制**（策划案定稿）：按当前玩法词条过滤能力池（第 1 层 = 基础词条 basic）→ 随机 3 个候选 → 三选一 + 每项可刷新一次（刷新池循环：优先排除已展示；池空回填但至少不回当前项）。
- **实现**：`RelicDef.tags`（词条）+ `RelicEffectSpec`（基础效果组合：APBonus/DrawExtra/PromoteCopyDeployable/ActionEconomy/DeployRow/DrawEditedImmediate）；`EventDefinition.isAbilityPick` + `AbilityCandidatesDrawn` 事件 + `GameState.AbilityCandidates/AbilityRefreshLeft`（入档）；`Resolver.DrawAbilityCandidates/RefreshAbilityCandidate/SelectAbility`。
- **基础池 5 条已配**（relics.json：ability_draw_extra / promote_copy / action_economy / deploy_row / **ability_edited_strike（E5 抽牌即战——2026-08-23 触发实现后补配）**——tags basic；已移除误配的 ability_ap 行动点强化——"行动点+1"为共用附带非独立能力，见决策记录勘误）。
- **效果**：APBonus（GrantRelic 即时）+DrawExtra（AP 抽牌额外）/PromoteCopyDeployable（升变部署→复制牌）/ActionEconomy（执行免 AP + 每棋子每回合一次——已行动集回合重置；免费行动=额外行动穿透）/DeployRow（己方部署区 +N 行）。
- **待续**：~~插入骨架~~ ✅ **已实现（2026-08-22）**：免费行动"获得即立即执行"（ExtraActionGranted → 插入队列 → 整段执行收尾后强制该棋执行——free 额外行动；E5 同链预留）；**前端**：三选一面板 + 刷新按钮（docs/前端待办「高优先级」）、行动经济 buff 显示、插入执行的 UI 形态（待沟通——后端为朴素强制选格）。
- **资产回退状态（2026-08-23）**：能力事件资产（`Event_ability_pick` + `Relic_ability_*` ×5）**临时回退取消注册**——原因：前端三选一面板未接，资产生效后能力事件走 isAbilityPick 分支（不发 EventOpened、无 UI 卡住）+ 新局预加载 BattlePanel 可见 → 表现为"点开始游戏立刻进入战斗界面"（详见 docs/前端待办.md 2026-08-23 补充条目）。回退范围：prefab 注册 + Settings/Configs 资产重写 + 新资产生成物；**保留**：收集工具泛化（「工具/收集全部配置到 Bootstrap」）+ 本待办工具条目。**前端面板完成后**：重跑「工具/导入关卡配置（JSON）」+「工具/收集全部配置到 Bootstrap」即恢复注册（无手工拖拽）。
- **编辑程序约束实施（2026-08-23——✅ 后端已实现；锁定语义待前端反馈）**：① ~~同类型同 id 模块唯一校验~~（**2026-08-23 晚撤销**——策划定案允许跨层叠加，程序内同 id 可多份；同层不重复由消耗制保证）；② **消耗制候选（每层净增量，策划定案"池子构成规则"）**——候选池 = 模板库 − 本层占用增量：放入=消耗（候选消失）、撤销/移除=返还（候选恢复）、`TowerFlow.EnterFloor` 进层清空（跨层复原、可再抽再用、可跨层叠加）；`ResetForNewRun` 清 + 入档；维护在唯一写入口（`Resolver.ApplyProgramEdit`——Undo/还原自动同步）；③ **棋子 Id 全局单调**——`_nextPieceId` 不随战斗/新局重置（`NextPieceId` 入档即记录位置；防 Id 复用串状态）；④ 生命周期清理——`HandleDeath` 清死亡棋子的预告/免费资格/已行动集、`ResetForBattle` 清免费资格/已行动集。前端配合项（锁定语义调整方向）见 docs/前端待办「编辑界面方案沟通」。

## 吐槽（2026-08-23）

> "整理文案那边需求的表格的时候，又发现了两个新的bug，然后连忙中断整理表格，开始改bug……感觉现有的bug和我钱包里的空气一样随处可见了……要是我的钱包能和AI的胆子一样肥嘟嘟的就好了。"

## 不在此文件跟踪

- 免费攻击立即插入、攻击槽选择、无攻击槽处理、资格叠加：见 `docs/待确认.md`。
- 即时失败、计分倍率来源、关卡计分配置、默认模块锁定、效果模块内容：见 `docs/待确认.md`。
- 历史契约与交接记录：见 `docs/archive/`。
