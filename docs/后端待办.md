# 后端待办（前端测试发现，统一记录待协作者处理）

> 记录人：UI 侧（前端）
> 状态说明：列表内各项为测试/对接中发现的问题，已定位根因与建议修法，待后端确认/处理。
> 更新（2026-08-06 后端处理）：第 1 条与第 3 条已修复（见各节状态）；第 2 条规划中。

---

## 1. 敌方单位攻击/移动方向未做阵营反向

**现象**：敌方单位（如短弓手）选中后显示的攻击范围是朝棋盘上方（远离玩家）——敌方在上方部署区（y 6~7），`AttackTemplate.directions = Up` 直射 3 格，范围全在敌方身后，永远打不到玩家。视觉上表现为"敌方和我方同向、向后射箭"。

**根因**：`BoardRules.GetAttackableCells`（以及 `GetLegalMoves`）把 `template.directions` / `MoveStep.direction` 直接按**棋盘格世界方向**解析（`DirectionToVector(Up) = (0,1)`），未按棋子阵营区分。数据层敌我共用同一套 PieceDef（基础牌组 12 张，升变牌玩家也可获得），**不能靠改数据**按阵营区分方向。

**建议修法（方案 A，最小改动）**：查询层对 `side == Enemy` 的棋子做方向集镜像翻转：

```
Up ↔ Down，Left ↔ Right，UpLeft ↔ DownRight，UpRight ↔ DownLeft
```

即敌方查询攻击/移动范围时，方向集自动反向（朝向我方），玩家侧保持不变。

- 位置：`Assets/_Project/Gameplay/BoardRules.cs` — `GetAttackableCells`（方向循环处）、`GetLegalMoves`（MoveStep.direction 处）
- 改动量：约 20 行，前后端共享代码一次生效

**备选方案 B（方向相对 facing）**：directions 改为相对棋子 facing 旋转（语义最正，但改动面大：攻击+移动+AI 目标选择全链路，且 `PieceInstance` 构造已按阵营翻转 facing——若采用本方案，facing 翻转改动正好配套）。是否采纳由后端定。

**附带说明**：`PieceInstance.cs` 构造已实现敌方初始 `facing` 翻转（Up↔Down），目前 facing 字段无战斗逻辑消费（仅入存档）——如后端采用方案 B 可直接利用。

**✅ 已修复（2026-08-06，方案 B 采纳）**：`BoardRules.RotateVector`——directions/MoveStep.direction 语义定为"相对棋子 facing（正前方）"，解析时按 facing 旋转到世界方向（Up 无旋转/Down 180°/Left 90°/Right -90°）；GetAttackableCells 与 GetLegalMoves 均应用；敌方 facing 已镜像（Up↔Down）→ 方向自动朝向我方。方向语义文档已同步（架构设计.md §六）。

---

## 3. 战斗结束后误判胜利（失败后又出现胜利日志）

**现象**：玩家失败（EndBattle → GameOver）后约 116ms，又触发一次 GameOver + 胜利判定（日志："战斗结束（失败）→ 返回主菜单"后接"战斗结束（胜利）→ 返回主菜单"）。

**根因**（三个叠加缺陷）：
1. **Bootstrap.BackToMainMenu 在规则层事件回调内同步 ResetForNewRun**——把 GameState.Phase 重置回 Placement，破坏 CheckVictory 开头的 `Phase == GameOver` 防御（BattleFlow.cs 约 590 行）
2. **AllWavesDeployed() 依赖 BattleFlow 字段 `_deployedWaveIndex`**——只被 StartBattle 重置，ResetForNewRun 不清 → 残留"3 波已部署" → 棋盘清空后误判全灭胜利
3. **ResolveEnemyTurn 的 foreach 无 GameOver 中断**——失败后仍处理剩余请求，每请求末尾 CheckVictory 重新判定

**触发链**：失败 EndBattle → OnStateChanged 回调 → BackToMainMenu → ResetForNewRun（Phase=Placement）→ 返回 ResolveEnemyTurn 剩余请求循环 → ProcessRequest 阶段校验被拒但末尾 CheckVictory 仍执行 → IsPlayerDefeated=false（手牌已填 12）+ AllWavesDeployed=true（残留）+ IsEnemyWiped=true（棋盘空）→ 判胜。

**修复建议**：
- **C（根本）**：BattleFlow 加 `_battleEnded` 标志——EndBattle 置 true / StartBattle 清 false；CheckVictory 开头 `if (_battleEnded || Phase == GameOver) return;`（约 3 行）
- **D（防御）**：ResolveEnemyTurn foreach 加 `if (_state.Phase == BattlePhase.GameOver) break;`
- **B（结构性，可排期）**：BackToMainMenu 不在规则层调用栈内同步重置状态（延后一帧）

**归属**：规则层时序漏洞（BattleFlow），共享代码。

**✅ 已修复（2026-08-06）**：
- **C（根本）已实施**：`BattleFlow._battleEnded` 标志——EndBattle 置 true / StartBattle 清 false；CheckVictory 双重防御（`_battleEnded || Phase == GameOver`）——标志位不可被 ResetForNewRun 重置，根治"重置破坏防御"
- **D（防御）已实施**：`ResolveEnemyTurn` foreach 加终态 break（`Phase == GameOver || _battleEnded`）
- **B（结构性）未做，排期**：BackToMainMenu 延后一帧——涉及 Bootstrap 回调时序与 UI 协作，另行排期

---

## 4. 事件关对接缺口（3 项，阻塞事件 UI）——✅ 已修复（2026-08-09）

**背景**：第 1 层节点序列含 3 个事件节点（ability/edit/deck），事件数据齐全（3 池 4 事件），EventNodeSystem 规则层已实现；UI EventPanel.prefab 布局已备，缺代码。以下三项不修无法跑通事件关。

**P1-1 资产名前缀不匹配（会崩）** ✅ 已修复
- 根因：JSON 引用字段写裸名（`ability_pool`/`relic_move`），资产名被导入器加前缀（`Pool_`/`Relic_`）——两套命名没对齐（数据契约未定义）
- 修复：**导入器幂等补前缀**——ImportFloor 的 eventPoolIds 补 `Pool_`、ImportEvents 的 relicName 补 `Relic_`（`NormalizeAssetName`：裸名补前缀、已带前缀原样——防重复导入叠加）。JSON 保持裸名（写数据直观），转换集中导入器一处，与池条目 eventId 补 `Event_` 前缀同一契约
- ⚠️ 弃用"资产名去前缀"（改名=新 GUID=断 Bootstrap 引用）

**P1-2 OpenEvent 不通知 UI** ✅ 已修复
- 修复：`GameEvent.EventOpened`（携带 CurrentEventId）——OpenEvent 末尾广播；UI 监听打开事件界面

**P1-3 事件选项处理后无推进链路** ✅ 已修复（方案 B：UI 确认推进）
- 修复：`GameEvent.EventCompleted`（UI 发）——TowerFlow 监听 → `AdvanceNode()`
- 为什么不用规则层自动推进：编辑/构筑事件（EditProgram/DeckBuild）打开 UI 界面让玩家交互，规则层不知道玩家何时完成——自动推进会在玩家还没用界面时就走到下一节点
- 模式与 PlacementFinished 一致：**UI 报告完成、规则层决定推进**（推进决策权始终在规则层）
- ⏳ **UI 侧待办（李毕）**：事件 UI 在事件交互完成后发 `EventCompleted`（无交互事件如拾取遗物——显示结果后发；编辑/构筑事件——玩家编辑/构筑完发）

**P2**：EventNodeSystem.OnTargetSelected 骨架未实现（targetRule 空的手动选目标效果）——⏳ 暂缓：当前 4 个事件无 targetRule 空的效果，无触发路径。

**⚠️ P3（高优先——阻塞事件 UI 文案，2026-08-09 前端强烈要求）事件文案字段缺失**：`EventDefinition` 只有 `options`——测试数据 JSON 的 title/description（"神秘遗物"/"路边石台上放着鹰眼符石…"）未导入（数据类无字段 + 导入器不读）。**事件 UI 当前只能显示资产名（如 ability_relic_move），玩家不可读**。修法：EventDefinition 加 `title`/`description` 字段 + ConfigImporter 导入（改动极小）。

**✅ 已修复（2026-08-11，前端实施）**：
- `EventDefinition` 加 `title` / `description`（TextArea）字段
- `ConfigImporter.ImportEvents` 写入两字段（JSON DTO 原已有，此前未赋值）
- `EventPanel.ShowEvent`：标题优先 title（空→资产名兜底）；描述优先 description（空→标题兜底）
- ⏳ **Unity 操作（待做）**：跑一次"工具 → 导入关卡配置（JSON）"把文案写入已有 4 个 Event_*.asset（增量模式 GUID 不变）——✅ **已执行（2026-08-11）**：4 个 Event_*.asset 已含 title/description（提交验证）

---

## 2. 程序块编号体系 + 前端描述表（规划中）
## 2. 程序块编号体系 + 前端描述表（✅ 编号已落地 2026-08-09，描述表重建待做）

**背景**：棋子行为 = 程序块（MoveTemplate/AttackTemplate/SkipTemplate）槽序列决定；行为描述应挂在**程序块**上（种类+编号，可复用），不按棋子写（棋子可能带特殊能力，不属于程序块行为）。

**现状**：✅ **编号已落地（2026-08-09）**——Template 基类加 `id` 字段；12 个棋子 JSON modules 已按结构分组编号（Move-1~9 / Attack-1~10，同结构共用同 id，见变更日志分配表）；导入器读 id 并改**增量模式**（资产已存在更新不删建——GUID 不变，场景引用不再断）。UI 描述仍为代码机械拼合（`SlotDetailDesc`）——待阶段 2。

**规划**：
1. ✅ 后端给程序块加编号（已完成：Template.id + JSON id + 导入器读 id）
2. ✅ **阶段 2（已完成 2026-08-11，前端实施）**：`SlotDescTable.FeatureOf` 改读 `Template.id`（key 如 "Move-1"）；`slot-descriptions.json` 已按编号重建（20 条：Move-1~9 + Attack-1~11，含 Attack-11），UI 查表显示 + 未命中回退代码生成
3. ✅ 描述表消费方已切换（BattleController.SlotDetailDesc / PieceEditPanel / SlotDescTable.Get 均查表优先）
