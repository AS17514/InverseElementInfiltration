# 后端进度说明（给前端）

> 更新：2026-08-20（初版）——**此后每次后端更新时同步本表**
> 目的：让前端清楚后端已实现/支撑了哪些策划规则功能，避免对后端进度模糊
> 原则：**一切以策划案为准**——本表只列策划要求的功能/规则与后端支撑；表现方式（特效/动画/展示形式）由前端按策划需要自定，不在此臆想。
> 前端对接契约（已给）：编辑候选双池与放回契约 / 抽牌堆机制与构筑规则 / 计分规则 / 音频资源需求单

---

## 一、已落地的策划功能（后端）

| 功能 | 后端已实现（策划规则） | 前端按策划需要支持 |
|---|---|---|
| **波次/敌方** | FloorConfig 波次（随机池/固定站位/间隔）；自动升变预告 + 随机升变 | 波次部署、升变预告提示（✅ 已做——78b1ecb） |
| **编辑事件** | 6 候选抽取 + 三选一（EditCandidatesDrawn/ConfirmEditPiece）；候选池（GetEditCandidates）；放回（TryRestoreBuiltinModule）；撤销快照 | 三选一面板（✅ 已做）；编辑面板接候选池/放回 |
| **构筑** | 满 12 张、可重复、升变≤初始（**价值上限已废除**——无价值限制） | 构筑面板（✅ 已做）；确认 ==12 |
| **抽牌堆** | 首回合自动抽 4；1 AP 抽 1；判负三条件 | 抽牌操作与抽牌堆剩余展示（✅ 已做——RefreshDrawPile） |
| **计分** | 回合计分制（基础分×倍率，**敌方回合末结算**——2026-08-20 修复时机）；本关独立；敌方棋子被击败 → 基础分 + 价值 | 基础分/倍率/总得分等状态展示（后端字段直读——展示形式前端定） |
| **玩法·属性** | 棋子属性分配（创建时随机/复制牌带属性）；相克击杀（无视护盾+得分）/相克升变（倍率+1）；相生（无伤+复制牌）/相生升变（复制牌）；**属性/特殊能力放 Buff 栏——策划确认可行（2026-08-20）**——后端已备属性描述（buffs-descriptions.json element_*） | 属性/能力以 Buff 栏展示（前端 BuffDisplay）；相克/相生结算结果需呈现给玩家（形式前端定） |
| **玩法·麻将** | 18 张进抽牌堆；牌山（≤2）与番数；墙体（阻挡/破坏填分）；行动：打出墙体/摸切/和牌 | 展示麻将牌/牌山番数/墙体；玩法操作（打出/摸切/和牌） |
| **敌方击杀扣分** | 开关 `ScoreDeductEnabled`（第 3/4 关配置启用）；我方棋子被**敌方棋子**击败 → 本关总得分 - 该棋子价值（clamp 0）；接口 `DeductScoreForPlayerLose` | 被扣分时展示（得分状态直读） |
| **音频** | AudioManager 框架已备；《音频资源需求单》已定稿 | 资源制作 + 挂点（待排期） |

## 二、前端落地支撑清单

**已接好**：三选一面板、抽牌操作与抽牌堆、构筑面板、升变预告提示。

**待前端（后端接口全部就绪——按策划规则所需）**：
- 编辑面板接候选池（GetEditCandidates）与放回（TryRestoreBuiltinModule）
- 麻将：展示麻将牌 / 牌山与番数 / 墙体；玩法操作（OnPlayerRequestPlayMahjong / Mochi / Hu）
- 属性：属性以 buff 项展示（BuffDisplay）；相克/相生结算结果呈现
- 得分状态展示（基础分 / 倍率 / 总得分——后端字段直读）
- 效果模块（Effect）展示与文案（Effect-N 描述已备）
- 免费行动**两层 buff**（**策划确认放 Buff 栏可行**——2026-08-20）：① 能力本体（击杀后免费行动一次——常驻）② 触发效果（当前有待执行的额外行动——临时）——后端 `GetAllAbilities` ExtraAction + `FreeExecutes` 状态 + `buffs-descriptions.json` 描述（能力 `ability_2119510590` / 触发 `free_execute`、`free_action_pending`）支撑

## 三、待策划/待定（前端可暂不跟进）

- 第 2-4 关配置（波次/达标线/目标分/玩法激活/扣分开关——策划输入）
- 玩法选择机制（改变规则事件怎么选玩法）——机制待定
- 玩法池目前 2 种（麻将/属性）——第 4 层第 3 种待扩充或重复
- 词条系统（能力按玩法过滤）——未实现

---

*后端更新流程：实现/变更后同步更新第一节表格 + 第二节清单。*
