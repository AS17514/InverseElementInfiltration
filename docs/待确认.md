# 待确认

> 当前唯一跨端 / 策划决策源。决议后，将实施项迁入 `前端待办.md` 或 `后端待办.md`。

## 战斗与规则

- [ ] **即时失败时机**：敌方行动过程中玩家棋子全部死亡时，立即判负，还是等敌方回合结束再判定？当前通常等回合收尾检查。
- [ ] **免费攻击资格叠加**：同一棋子连续获得资格时，是否每次增加一层？当前 `HashSet` 只能保留一层。
- [ ] **计分倍率来源**：倍率由遗物、效果模块，还是两者共同提供？
- [ ] **计分配置**：补齐第 2/3 关每波达标线，以及第 2/3/4 关 `targetScore`、胜利规则、敌方阵容等配置。

## 免费攻击

- [ ] **免费攻击执行槽位**：立即攻击时执行第一个攻击槽、全部攻击槽，还是让玩家选择攻击槽？当前后端仍是“保留资格到下次完整执行”。
- [ ] **无攻击模块处理**：触发免费攻击的棋子没有攻击模块时，直接消费并跳过，还是保留资格 / 禁止触发？通常不会出现，但需定案。
- [ ] **免费攻击表现边界**：已确认“获得后立即锁定棋子、打开目标选择、允许空放”；仍需确认资格消费时机、空放表现以及与敌方回合/表现队列的衔接。（2026-08-24 补充定案：插入执行 UI 形态 = 提示条 + 自动锁定棋子 + 弹目标选择 + 允许空放；资格消费时机跟随后端 `FreeExecutes` 机制。）

## 编辑事件与程序块

- [ ] **默认行动模块锁定语义（✅ 已确认 2026-08-24 用户：默认模块全部可编辑（不锁定），被替换的默认模块回到候选库）**：回库需后端候选池差集动态化（已记入 `docs/后端待办.md`）。**⚠️ 范围修正（2026-08-24「取消效果编辑」定案）**："全部可编辑" = **移动 + 攻击**；**效果模块除外**——默认程序内置效果保留原位、不可移除/替换、不进候选池（效果槽锁定，前端配合恢复）。
- [ ] **消耗制候选的移除周期语义（✅ 已确认 2026-08-23 策划：每层消耗、跨层复原）**：编辑面板程序库"消耗制候选"作用域 = **每一层**：本层编辑事件用过即消耗（候选不再显示该模块）；**进入下一层消耗复原**（池子完整，仍可抽到并再次使用）；跨层可用同一模块（非单次事件内重复使用，允许叠加——程序内同 id 可多份）；**撤销/移除后候选恢复**（"没有东西再用了就要允许重新出来"）；价值**重复模块重复计值** ✅ 已确认。实施完成（后端"每层净增量"——Resolver.ApplyProgramEdit 唯一写入口维护 + GetEditCandidates 过滤 + EnterFloor 清空 + 入档）。
- [ ] **Effect-4+ 外部效果模块定义（✅ 已关闭 2026-08-24：取消效果编辑）**：效果模块不再进入编辑候选（编辑事件候选 = 移动/攻击各 2），外部效果模块（Effect-4+）无编辑入口——定义失去意义。Tpl_Effect_4 等外部效果资产保留在模板库（无候选入口 = 死数据，同 Attack-11 先例标注）。

## 战斗 UI 与牌库

- [ ] **Txt_Abilities 展示范围**：当前第一版绑定为当前选中单位的固有能力、效果模块能力、临时能力；是否改为只显示本次事件获得效果，或增加独立的全局事件效果节点？
- [ ] **弃牌堆语义（✅ 已确认 2026-08-24 用户定案 + 设计原文佐证：弃牌区 = 墓地，同一概念；**DiscardZone 已实施落地**）**：出处《游戏方案描述》§4"死亡棋子进**墓地/弃牌堆**"——玩家牌死亡进墓地（`GameState.Graveyard`，记死亡时 defId = 升变后形态）；**升变瞬间被升变的原牌立刻进弃牌区**（2026-08-24 策划新语义），升变棋子死亡时弃牌区只记升变牌（当前形态）；**弃牌区大容器 `DiscardZone`**（2026-08-24 实施）：棋子死亡 Card 化 + 麻将死亡（摸切立即/墙体破坏后）——与原 Graveyard 双写 A；围棋死亡不进弃牌区（"棋子牌"未消耗）。后端数据源/接口清单见 `docs/前端待办.md`「牌库面板」补充（读取方式：只读快照 + 现有事件，无新传参要求）；展示与否由前端按需决定。
- [ ] **牌库面板·抽牌堆显示（✅ 已拍板 2026-08-25 用户：C1——全部牌正面显示 + 每次打开随机打乱）**：抽牌堆（`GameState.DrawPile`——**随机抽取无牌序**）在面板中显示形态：**C1 = 堆内全部牌正面展示（棋子牌立绘+名 / 麻将牌也显示正面点数）+ 每次打开面板/切页时随机打乱排列**（不暗示抽取顺序——诚实反映随机抽取语义）；顶部数量角标；无"下一张"暗示。实施责任：**前端 DeckLibraryPanel**（后端零改动——数据已公开）；此条为决策记录 + 前端待办契约，前端照做即可。

## 已确认决策

- WipeOut 胜利语义按关卡配置决定；后端待办负责提供配置化实现。
- 编辑候选当前使用事件级模块池：移动 / 攻击 / 效果各抽 2 个，无放回；池不足时实际数量可少于 6。外部候选由事件共享，内置差集按棋子计算。
- 敌方升变预告只显示在棋子本身：肖像描边 / 脉冲；顶部或波次区域不额外显示。新手教程解释升变波次概念。
- 谢幕面板是首次通关后的独立 Thanks for playing / 结局报幕，暂缓制作。
- `OnTargetSelected` 保留为长期能力，不列近期开发任务。
- 计分常规结算时机为敌方回合结束；失败、终局、强制判定允许必要兜底。
- 新玩法激活（2026-08-24 定案，**以最新为准——此前"关卡顺序固定、无玩法选择过程"记录作废**）：玩法事件二选一——从未激活玩法（麻将/属性/骰子/围棋/代币 5 个）随机抽 2 → 玩家二选一、**不可刷新**；**已选排除**（候选池 = 全部玩法 − 已激活，已激活玩法后续不再出现）；**落选保留**（后续仍可能再次出现）；第 2/3/4 关各 1 次玩法事件（rule 节点须在 ability 节点之前）；后端已实施（`RuleCandidates`/`RuleCandidatesDrawn`/`SelectRule`——见 `docs/后端待办.md`）。战斗中显示已激活玩法（未选中=显玩法、选中=显属性、取消恢复——与棋子信息区同位置切换显隐）= 前端可选项，见 `docs/前端待办.md`「玩法事件·二选一面板」。

## 说明

- 历史归档文档只用于本轮审计，不再作为任务状态源。
- TemplateDef 架构已实际启用；当前后端任务是补齐缺失的 Attack-11，而不是重新决定是否资产化。

## 新增待决策（2026-08-26 最终验收）

- [x] **骰子移动取消入口（AA4-09）——已实现（2026-08-26 前端轮）**：前端无可达方向 → 调 BattleFlow.OnPlayerCancelDiceMove（清等待+buff）+ 回合状态按钮处提示。
- [ ] **ProgressSystem 去留（AA5-13）**：全项目无调用方——保留（未来计分用途）还是移除。
- [ ] **A7 配置人工核对（4 项）**：旧遗物事件死链（ability_relic_range/move 未挂池 + 遗物无 tags）、scoreDeductEnabled 与 FloorConfig.cs:16 注释及 map.json 文案冲突、Tpl_Effect_4 死模板（效果编辑已取消）、悬空描述条目（Attack-11/free_action_pending）。