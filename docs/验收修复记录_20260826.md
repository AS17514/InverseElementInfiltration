# 最终验收·修复记录（2026-08-26）

> 背景：全项目最终验收审计（第一轮只读 8 区，共 76 项发现）→ 第二轮按"先 BUG 后其余"分批修复。
> 审计详情：`C:\Users\KAI\AppData\Local\Temp\iei-final-audit\r1\`（A1~A8 分区报告 + consolidated.md）。
> 修复详情：`C:\Users\KAI\AppData\Local\Temp\iei-final-audit\r2\`（fix_battle / fix_save / fix_editor）。

## 一、打包修复（可玩性——本会话 + 并发会话）

| 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|
| 玩家包零 Addressables 内容（立绘/面板/音频/字体全挂） | `BuildAddressablesWithPlayerBuild=0` → 玩家构建跳过内容 | 置 1（`AddressableAssetSettings.asset`） | 打包后 StreamingAssets/aa 含 6 bundle、95MB，运行 0 异常 |
| TMP 默认字体打包后丢失（紫字 + 布局 NRE 风暴） | 字体注册进 Addressables 组 → TMP Settings（Resources）直引断裂 | 字体移出 Addressables 组，回普通构建 | 运行仅剩教程面板 1 处 prefab 未配字体（既有缺陷，另记） |
| 主菜单按钮精灵品红块 | 按钮背景精灵仅作隐式依赖，Sprite 子资产未进包 | 显式注册 `popup_confirm_cancel_btn` 进 UIPanels 组 | 按钮灰底白字正常（残留：场景装饰紫块，见遗留③） |
| 棋盘点击被 UI 拦截（疑"没法玩"主因） | 全屏 Background / 场景装饰图 raycastTarget=1 | 并发会话已修：运行时将面板 Background 与 `Img_Foreground/Img_Board/Img_Stand` raycastTarget 置 false | 见并发会话改动 |

## 二、BUG 轮修复（8 项 + 1 扩展，全部编译 0 error）

| id | 问题 | 修复 | 文件 |
|---|---|---|---|
| AA4-01 | 选格/执行/表现等待中可"结束回合"→ 免扣 AP + 半程序丢弃 | 后端 OnPlayerEndTurn 空闲守卫；前端 OnPhaseButtonClicked 同步拦截 | BattleFlow.cs / BattleController.cs |
| AA4-01+ | 骰子点数移动选方向等待态未被拦截（同类） | 后端 `_waitingDiceMovePieceId>=0`、前端 `_diceMoveSelecting` 并入守卫 | 同上 |
| AA4-04 | 围棋部署无界内校验（盘外棋/判负失效） | BattleFlow 请求条件 + Resolver.DeployGoPiece 双入口补 IsInsideBoard | BattleFlow.cs / Resolver.cs |
| AA4-05 | 麻将打墙无界内校验（越界墙消耗手牌） | PlayMahjongWall 取牌前补 cell/second 界内校验 | Resolver.cs |
| AA4-06 | 敌方击杀额外行动被下一决策覆盖截断单槽 | TryNextEnemyDecision 入口空闲守卫（等收尾再触发） | BattleFlow.cs |
| AA4-02 | 读档 NextPieceId 缺省 0 → 棋子 Id 冲突 | 棋子恢复后 `<=0` 时取最大 id+1 兜底 | GameState.cs |
| AA4-03 | 读档 WaveIndex 缺省 0（注释预期 -1）→ 得分串波 | PieceDto.WaveIndex 改可空，读档 `?? -1` | GameState.cs |
| AA6-2 | 字体工具默认字体指向已删资产（一点即置空） | 改为加载现存 SDF_48，失败 LogError 不置空 | FontAssetCompleter.cs |
| AA6-3 | 能力指纹 Passive 忽略参数（不同被动共享资产） | 补 `Ability_Passive_{target}_{value}` 与 ConfigImporter 对齐 | PieceConfigImporter.cs |

说明：
- AA8-01（棋盘点击拦截）已由并发会话修复（见上表），本会话未重复。
- 全部改动遵循最小改动；未触碰他人未提交 WIP；行尾统一 LF（BattleController.cs 工作副本存在 CRLF，git 提交时按 .gitattributes 规整，不阻塞）。
- 编译验收：`unity_get_compilation_errors`（port 7891）0 error。


## 三、RISK 轮修复（第三轮·全部 25 项落地，编译 0 error）

| 批 | 项 | 结论 |
|---|---|---|
| 战斗 | AA4-07（重置清单补瞬态）/ AA4-08（波次部署表现回执后补触发敌方决策）/ AA4-15（附着伤害补发 DamageDealt） | ✅ 已修复 |
| 存档 | AA5-01（tutorial.json 容错）/ 02（TempAbilities 兜底）/ 03（LoadAll 逐段隔离）/ 04（原子替换，含编译兼容修正）/ 07（诊断联动 isDebugBuild）/ 08（楼层越界防御）/ 09（空池安全推进）/ 10（权重全 0 等权兜底）/ 11（RestoreAll 清理）/ 14（旧档教程迁移）+ AA3-02（RNG 计数一致）/ 06（写盘降级）/ 07（音量钳制） | ✅ 已修复 |
| UI | AA1-03/04/07/08 | ✅ 已由并发会话修复（核验通过） |
| UI | AA8-02（TutorialManager 监听 TutorialRequested） | ✅ 已修复 |
| 编辑器 | AA6-4（收集去重）/ 5（跳过 0 id）/ 7（地址冲突检测）/ 9（导入隔离+finally SaveAssets） | ✅ 已修复 |

### RISK 未修项（记录在案）
- **AA2-11/AA2-12**（StoryTransition tween 清理 / TooltipManager 根 Canvas 生命周期）：单场景架构下不可达，记录不修。
- **AA4-09**（骰子移动无可达方向卡回合）：需后端新取消入口，待设计决策（建议进 docs/待确认.md）。
- **AA5-13**（ProgressSystem 全程无调用方）：死功能去留需决策。
- **A7 配置类 4 项**（遗物事件死链 / scoreDeductEnabled 注释冲突 / Tpl_Effect_4 死模板 / 悬空描述）：配置资产范围，走人工/李毕。

### 新发现（记录）
- TutorialManager 旧事件观察式触发（EventOpened/PhaseChanged 等 → ShowSequence）与 TutorialRequested 双路径并存，仅会话内去重、绕过 TutorialSystem 跨局持久去重——建议后续以 TutorialRequested 收口。

## 四、遗留事项

1. **主界面背景装饰紫块**（打包后场景装饰图纹理缺失，右上横排 4+ 块）——移交打包分叉排查。
2. **教程面板 Txt_Text 未配字体**（TutorialPanel.prefab，prefab 既有缺陷，非打包回归）——属李毕 prefab 范围，建议后续修。
3. **审计后续轮次**：PERF 23 / DEFENSIVE_CLEANUP 10 未修（清单见 r1/consolidated.md），BUG 与 RISK 轮已完成。
4. **文档回填**：docs/前端待办.md、docs/后端待办.md 待回填本轮结果（按用户指示以本文档为最终记录）。